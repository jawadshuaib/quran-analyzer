"""Phase 0 — the pre-Islamic poetry corpus.

This is the prerequisite for the whole Qurʾān↔poetry comparison feature
(see ../../docs/pre-islamic-poetry-implementation.md). Before any agent can
draft "how does root K-F-R differ in the poetry?", the system needs the
poetry: acquired, tiered for trustworthiness, and indexed by root.

It is a SELF-CONTAINED CLI (raw sqlite3 — it does NOT import app.py, so it
stays fast and never triggers the IDF engine build). It owns three tables:

    poetry_poems        one row per poem  (poet, era, meter, rhyme, auth_tier)
    poetry_lines        one row per bayt  (two hemistichs)
    poetry_line_roots   the root index    (LLM-extracted; humans verify quotes)

Authentication ladder (the objectivity backbone — see the research doc §2.2):
    A  Muʿallaqāt (hand-curated / promoted)        -> strong contrast claims
    B  critical anthologies, major Jahilī poets    -> normal contrast claims
    C  raw scraped corpus (Kaggle, era=jahili)      -> statistics + discovery only
    D  disputed                                     -> teaching the problem only

Usage:
    python poetry_corpus.py init
    python poetry_corpus.py load-muallaqat                      # data/poetry/muallaqat.json
    python poetry_corpus.py load-kaggle data/poetry/arabic-poetry.csv
    python poetry_corpus.py tiers                               # promote Muʿallaqāt / major poets
    python poetry_corpus.py index-root kfr --dry-run
    python poetry_corpus.py index-root kfr                      # LLM root extraction
    python poetry_corpus.py verify-root kfr                     # list candidates for human check
    python poetry_corpus.py verify-root kfr --set 12 --set 15   # mark line_root ids verified
    python poetry_corpus.py stats

index-root needs a Claude API key (admin_preferences.claude_api_key or
CLAUDE_API_KEY env). The loaders/tiers/stats do not.
"""
from __future__ import annotations

import argparse
import csv
import json
import os
import re
import sqlite3
import sys
import time
import unicodedata

import requests

try:
    from buckwalter import buckwalter_to_arabic, space_root
except Exception:  # pragma: no cover - fallback if run from elsewhere
    sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
    from buckwalter import buckwalter_to_arabic, space_root

# ------------------------------------------------------------------------
# Config
# ------------------------------------------------------------------------

DB = os.path.join(os.path.dirname(os.path.abspath(__file__)), "data", "quran.db")
POETRY_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "data", "poetry")
DEFAULT_MUALLAQAT_JSON = os.path.join(POETRY_DIR, "muallaqat.json")

ANTHROPIC_URL = "https://api.anthropic.com/v1/messages"
DEFAULT_MODEL = "claude-opus-4-8"
MAX_TOKENS = 4000
MAX_ATTEMPTS = 3
LINES_PER_CHUNK = 80  # how many abyāt per extraction call
EXTRACT_MODEL = "claude-sonnet-4-6"  # extraction is mechanical; Sonnet is plenty + cheap

TIER_RANK = {"A": 3, "B": 2, "C": 1, "D": 0}

# Distinctive (normalized) name tokens for the Muʿallaqāt poets, used by the
# `tiers` command to promote scraped poems. Matching is substring-on-normalized
# so it tolerates al-/ibn/diacritic variants. Combined with era=jahili this is
# safe enough for a heuristic promotion; the human reviewer is the final gate.
MUALLAQAT_POET_KEYS = [
    "القيس",    # Imruʾ al-Qays
    "طرفه",     # Ṭarafa b. al-ʿAbd
    "زهير",     # Zuhayr b. Abī Sulmā
    "لبيد",     # Labīd b. Rabīʿa
    "كلثوم",    # ʿAmr b. Kulthūm
    "عنتره",    # ʿAntara b. Shaddād
    "حلزه",     # al-Ḥārith b. Ḥilliza
    "النابغه",  # al-Nābigha al-Dhubyānī  (the "extra" odes)
    "الاعشي",   # al-Aʿshā
    "الابرص",   # ʿAbīd b. al-Abraṣ
]

# Broader major Jahilī poets -> Tier B (anthology-grade attribution).
MAJOR_JAHILI_POET_KEYS = [
    "السموال",  # al-Samawʾal
    "الشنفري",  # al-Shanfarā
    "تابط",     # Taʾabbaṭa Sharrā
    "المهلهل",  # al-Muhalhil
    "دريد",     # Durayd b. al-Ṣimma
    "الخنساء",  # al-Khansāʾ
    "حاتم",     # Ḥātim al-Ṭāʾī
    "الحطيئه",  # al-Ḥuṭayʾa
    "علقمه",    # ʿAlqama al-Faḥl
    "المتلمس",  # al-Mutalammis
]

# Common bare (diacritic-free) surface forms per pilot root — used as a
# tight, high-precision prefilter for the AGENT-DRIVEN indexing loop so each
# batch is mostly real candidates (works for weak/doubled roots too, unlike
# the ordered-subsequence prefilter). Substring match on bare line text.
SURFACE_FORMS = {
    "kfr": ["كفر", "كافر", "كوافر", "كفار", "كفور", "كفرة", "تكفر", "يكفر",
            "نكفر", "اكفر", "مكفر", "كفران", "الكفر", "كفرت", "كافرين"],
    "wqy": ["وقى", "يقي", "تقي", "اتقى", "يتقي", "تتقي", "اتقوا", "تقوى",
            "التقوى", "تقاة", "واق", "وقاية", "اوقى", "توقى", "متقي", "المتقين", "اتقاء"],
    "dhr": ["دهر", "الدهر", "دهور"],
    "krm": ["كرم", "كريم", "كرام", "الكرام", "اكرم", "مكرم", "مكارم", "كرماء",
            "تكرم", "يكرم", "كرامة", "اكرام", "مكرمة", "كريمة"],
    "jnn": ["جنون", "مجنون", "جنة", "جنان", "اجن", "يجن", "تجن", "استجن",
            "جنين", "اجتن", "مجن", "الجن", "جان", "جنه"],
    # ---- scaling batch 3 (recall-first; precision supplied during indexing) ----
    "rbb": ["ربي", "ربك", "ربه", "ربها", "ربهم", "ربنا", "ربكم", "الرب",
            "ارباب", "اربابا", "ربة", "ربات", "مربوب", "ربوبية", "رببت"],
    "rzq": ["رزق", "الرزق", "رزقا", "رزقه", "رزقي", "رزقنا", "ارزاق",
            "الارزاق", "يرزق", "ترزق", "نرزق", "رازق", "الرازق", "مرزوق", "رزقت", "رزاق"],
    "rHm": ["رحم", "الرحم", "ارحام", "الارحام", "رحمة", "الرحمة", "رحمه",
            "رحمها", "رحيم", "الرحيم", "رحمن", "الرحمن", "رحماء", "ارحم",
            "يرحم", "ترحم", "مرحوم", "مرحمة", "رحمت", "استرحم"],
    "Sdq": ["صدق", "الصدق", "صادق", "الصادق", "صادقا", "صادقين", "صدقا",
            "صدقه", "صديق", "الصديق", "اصدقاء", "صدقت", "يصدق", "تصدق",
            "مصدق", "صدوق", "صداقة", "اصدق", "صدقني"],
    "Hsb": ["حسب", "الحسب", "حسبه", "حسبي", "حسبك", "حسبنا", "حسبهم",
            "احساب", "الاحساب", "حساب", "الحساب", "حسيب", "يحسب", "تحسب",
            "احسب", "حسبت", "محسوب", "حسبان", "حسابه", "حسبما"],
    "gfr": ["غفر", "غفور", "الغفور", "غفار", "الغفار", "غفرت", "يغفر",
            "تغفر", "استغفر", "استغفار", "مغفرة", "المغفرة", "غفران", "اغفر",
            "مغفر", "المغفر", "غافر", "يستغفر", "مغفور"],
    # ---- scaling batch 4 (recall-first; precision supplied during indexing) ----
    "Hmd": ["حمد", "الحمد", "حمدا", "حامد", "الحامد", "محمود", "يحمد",
            "تحمد", "نحمد", "حمدت", "حميد", "الحميد", "محمدة", "حمدته",
            "محامد", "تحميد", "يحمده", "حماد"],
    "hdy": ["هدى", "الهدى", "هداه", "هداها", "هداك", "هدانا", "يهدي",
            "تهدي", "نهدي", "اهتدى", "مهتد", "هاد", "الهادي", "هادي",
            "هدية", "الهدية", "هدايا", "هدي", "مهدي"],
    "xlq": ["خلق", "الخلق", "خلقه", "خلقها", "خلقنا", "يخلق", "تخلق",
            "خالق", "الخالق", "مخلوق", "خلقت", "خلائق", "الخلائق", "خلقة",
            "اخلاق", "الاخلاق", "خلوق", "خلاق"],
    # ---- scaling batch 5 (recall-first; precision supplied during indexing) ----
    "nwr": ["نور", "النور", "نوره", "نورها", "منير", "منيرة", "مستنير",
            "انار", "انارت", "تنور", "نار", "النار", "نارا", "ناري",
            "نيران", "النيران", "منار", "منارة", "استنار", "تنير"],
    "mlk": ["ملك", "الملك", "ملكه", "ملكها", "ملوك", "الملوك", "مليك",
            "المليك", "مالك", "المالك", "مملوك", "ملكت", "يملك", "تملك",
            "ملكا", "مملكة", "ممالك", "ملكوت", "املاك", "ملكي"],
    "Hkm": ["حكم", "الحكم", "حكمه", "حكمها", "حكيم", "الحكيم", "حكماء",
            "حكمة", "الحكمة", "احكم", "يحكم", "تحكم", "حاكم", "الحاكم",
            "محكم", "حكومة", "تحكيم", "حكمت", "حكام"],
    # ---- scaling batch 6 (recall-first; precision supplied during indexing) ----
    "Ezz": ["عزة", "العزة", "عزيز", "العزيز", "اعز", "الاعز", "يعز", "تعز",
            "عزت", "معتز", "عزيزة", "عزك", "عزي", "عزه", "عزها", "عزنا",
            "اعزة", "الاعزة", "عزيزا"],
    "qdr": ["قدر", "القدر", "قدره", "قدرها", "قدرة", "القدرة", "قادر",
            "القادر", "مقدور", "قدير", "القدير", "يقدر", "تقدر", "مقدار",
            "اقدار", "تقدير", "قدرت", "مقتدر", "قدروا"],
    "wHd": ["وحد", "وحده", "وحدها", "وحدي", "واحد", "الواحد", "واحدة",
            "احد", "الاحد", "وحيد", "الوحيد", "توحد", "موحد", "وحدان",
            "وحدانية", "وحدك", "وحدنا", "اوحد"],
    # ---- scaling batch 7 (recall-first; precision supplied during indexing) ----
    "ELm": ["علم", "العلم", "علمه", "علمت", "عالم", "العالم", "عليم", "العليم",
            "علماء", "معلوم", "يعلم", "تعلم", "نعلم", "اعلم", "تعليم", "علام",
            "معلم", "يعلمون", "تعلمون", "العالمين"],
    "$hd": ["شهد", "شاهد", "الشاهد", "شهيد", "الشهيد", "شهداء", "شهادة", "الشهادة",
            "يشهد", "تشهد", "اشهد", "مشهد", "مشهود", "شواهد", "شهود", "استشهد",
            "شهدت", "شهدوا", "نشهد", "يشهدون"],
    "Hyy": ["الحي", "حياة", "الحياة", "يحيا", "تحيا", "يحيي", "تحيي", "نحيي",
            "استحيا", "حياء", "محيا", "المحيا", "الاحياء", "احياء", "الاحيا",
            "حيوان", "نحيا", "حييت", "يستحي", "المحيي"],
    "nSr": ["نصر", "النصر", "نصره", "نصرة", "ناصر", "نصير", "النصير", "انصار",
            "الانصار", "ينصر", "تنصر", "انصر", "منصور", "نصرت", "استنصر", "ينصرون",
            "نصرنا", "ناصرين", "ننصر", "نصروا"],
    "Ehd": ["عهد", "العهد", "عهده", "عهدها", "عهدت", "عهود", "معهد", "يعهد",
            "تعهد", "عاهد", "عهدي", "عهدنا", "عهدكم", "معاهد", "العهود", "عهدا",
            "يعاهد", "تعاهد", "عهدته"],
    "wEd": ["وعد", "الوعد", "وعده", "وعدها", "وعدت", "وعيد", "الوعيد", "موعد",
            "الموعد", "يعد", "تعد", "اعد", "وعدنا", "وعدكم", "مواعيد", "ميعاد",
            "الميعاد", "موعود", "وعدوا"],
    "nEm": ["نعمة", "النعمة", "نعم", "النعم", "نعمه", "نعماء", "نعيم", "النعيم",
            "انعم", "ينعم", "تنعم", "منعم", "نعمت", "نعماك", "نعماه", "انعام",
            "النعماء", "متنعم", "ناعم", "نعمى"],
    "gny": ["غنى", "الغنى", "غني", "الغني", "غنية", "اغنى", "يغني", "تغني",
            "استغنى", "اغنياء", "الاغنياء", "مغني", "غنيت", "غنينا", "غنيا",
            "الغنية", "يستغني", "اغنيتك", "مستغن"],
    "kbr": ["كبر", "الكبر", "كبير", "الكبير", "كبراء", "كبرى", "اكبر", "الاكبر",
            "يكبر", "تكبر", "تكبير", "متكبر", "استكبر", "كبرت", "الكبراء", "اكابر",
            "كبار", "كبرياء", "مستكبر"],
    "xld": ["خلد", "الخلد", "خالد", "الخالد", "مخلد", "خلود", "الخلود", "يخلد",
            "تخلد", "خلدت", "خوالد", "مخلدون", "يخلدون", "تخليد", "خالدين", "خالدا",
            "خلدوا", "الخوالد", "خالدون"],
    "Hrm": ["حرم", "الحرم", "حرام", "الحرام", "حرمة", "حريم", "محرم", "محروم",
            "يحرم", "تحرم", "حرمت", "حرمات", "الحرمات", "تحريم", "محرمة", "حرمان",
            "الحرمين", "احرام", "حرمنا"],
    "Amn": ["امن", "الامن", "امان", "الامان", "امين", "الامين", "مؤمن", "المؤمن",
            "مؤمنين", "المؤمنين", "ايمان", "الايمان", "يؤمن", "امنوا", "امانة",
            "مامون", "أمن", "آمن", "إيمان", "أمين", "أمان", "يؤمنون", "مؤمنون"],
    # ---- scaling batch 8 (recall-first; precision supplied during indexing) ----
    "jbr": ["جبار", "الجبار", "جبروت", "يجبر", "تجبر", "متجبر", "تجبار",
            "جبابرة", "الجبابرة", "جبرية", "جبرت", "مجبر", "انجبر", "اجبر",
            "جبيرة", "جبارا", "تجبرت", "يتجبر"],
    "qhr": ["قهر", "القهر", "قاهر", "القاهر", "قهار", "القهار", "يقهر", "تقهر",
            "مقهور", "قهرت", "قهره", "قواهر", "قهرنا", "قهروا", "قهرته", "قهرا",
            "يقهرون", "مقهورين"],
    "slT": ["سلطان", "السلطان", "سلطة", "سليط", "تسلط", "مسلط", "سلطه", "سلطانه",
            "سلاطين", "يسلط", "تسليط", "سلطانا", "سلطت", "المسلط", "سلطنا",
            "تسلطوا", "سلطانهم", "المتسلط"],
    "xwf": ["خوف", "الخوف", "خاف", "يخاف", "تخاف", "نخاف", "اخاف", "خائف",
            "مخوف", "خوفا", "خيفة", "الخيفة", "مخافة", "المخافة", "خوفه",
            "خوفت", "يخافون", "خوفناهم"],
    "fwz": ["فوز", "الفوز", "فاز", "يفوز", "تفوز", "فائز", "الفائز", "فوزا",
            "فازوا", "فوزه", "الفائزين", "فائزون", "يفوزون", "فزت", "نفوز",
            "مفازة", "فزنا"],
    "smE": ["سمع", "السمع", "سامع", "السميع", "سميع", "سماع", "يسمع", "تسمع",
            "نسمع", "اسمع", "مسموع", "سمعت", "سمعوا", "مسمع", "سماعة", "يسمعون",
            "استمع", "سمعه", "اسماع"],
    "bSr": ["بصر", "البصر", "بصير", "البصير", "بصيرة", "البصيرة", "ابصر", "يبصر",
            "تبصر", "مبصر", "بصرت", "ابصار", "الابصار", "بصائر", "تبصرة",
            "استبصر", "بصره", "ابصروا", "مبصرة"],
    "wfy": ["وفاء", "الوفاء", "وفى", "وفي", "الوفي", "يفي", "تفي", "اوفى",
            "يوفي", "وفيت", "وافي", "موف", "وفاه", "الموفي", "استوفى", "وفوا",
            "اوف", "يوفون", "وفية", "الاوفى"],
    "HfZ": ["حفظ", "الحفظ", "حافظ", "الحافظ", "حفيظ", "الحفيظ", "يحفظ", "تحفظ",
            "احفظ", "محفوظ", "حفظت", "حفاظ", "الحفاظ", "حفظه", "محافظة",
            "حفيظة", "يحفظون", "احتفظ", "حفظنا"],
    "wkl": ["وكل", "توكل", "يتوكل", "الوكيل", "وكيل", "وكلت", "موكل", "اتكل",
            "يتكل", "توكيل", "المتوكل", "وكلاء", "وكله", "توكلت", "يوكل",
            "متوكل", "وكلنا", "المتوكلين"],
    "rfE": ["رفع", "الرفع", "رافع", "الرافع", "رفيع", "الرفيع", "يرفع", "ترفع",
            "ارفع", "مرفوع", "رفعت", "رفعة", "الرفعة", "رفعنا", "يرفعون",
            "مرتفع", "رفعه", "رافعة", "ترفعت"],
    "Elw": ["العلا", "العلو", "العلي", "الاعلى", "اعلى", "يعلو", "تعلو", "علوت",
            "علاء", "علياء", "المعالي", "تعالى", "استعلى", "علوا", "عاليا",
            "تعلوا", "معلاة", "العلياء", "يعلون"],
    # ---- scaling batch 9 (recall-first; precision supplied during indexing) ----
    "shfE": ["شفع", "الشفاعة", "شفاعة", "شفيع", "الشفيع", "يشفع", "تشفع", "شافع",
             "الشافع", "شفعاء", "مشفع", "شفعت", "يشفعون", "شفعه", "استشفع",
             "الشافعين", "شفاعتهم"],
    "dEw": ["دعا", "الدعاء", "دعاء", "يدعو", "تدعو", "ندعو", "ادعو", "داع",
            "الداعي", "دعوة", "الدعوة", "مدعو", "دعوت", "دعاه", "يدعون", "ادعوه",
            "دعوى", "تدعون", "استدعى"],
    "sjd": ["سجد", "السجود", "سجود", "ساجد", "الساجد", "يسجد", "تسجد", "اسجد",
            "مسجد", "المسجد", "سجدة", "سجدت", "سجاد", "سواجد", "يسجدون",
            "ساجدين", "مساجد", "سجده"],
    "Thr": ["طهر", "الطهر", "طاهر", "الطاهر", "طهور", "طهارة", "يطهر", "تطهر",
            "مطهر", "طهرت", "اطهار", "مطهرة", "تطهير", "المطهرين", "طهره",
            "طاهرة", "يتطهر", "طهورا"],
    "fqr": ["فقر", "الفقر", "فقير", "الفقير", "فقراء", "الفقراء", "افتقر", "يفتقر",
            "فاقة", "مفتقر", "فقرت", "فقره", "افقر", "مفاقر", "مفقر", "فقيرا"],
    "qrb": ["قرب", "القرب", "قريب", "القريب", "قربى", "القربى", "يقرب", "تقرب",
            "اقترب", "مقرب", "قربان", "القربان", "قربت", "قرابة", "اقارب",
            "الاقربين", "قربه", "مقربون"],
    "Hmy": ["حمى", "الحمى", "حمية", "يحمي", "تحمي", "احمي", "حام", "الحامي",
            "حماية", "محمي", "حميت", "حمايته", "حماه", "يحمون", "استحمى",
            "الحمية", "محمية", "حماها"],
    "lTf": ["لطف", "اللطف", "لطيف", "اللطيف", "لطفه", "لطافة", "يلطف", "تلطف",
            "لطائف", "ملاطفة", "لطفت", "لطيفة", "الطف", "لطفا", "متلطف", "لطفها"],
    "wsE": ["وسع", "الوسع", "واسع", "الواسع", "وسيع", "وسعة", "سعة", "اوسع",
            "موسع", "وسعت", "يوسع", "وسعها", "متسع", "واسعة", "وسعه", "اتسع"],
    "nfs": ["نفس", "النفس", "نفسي", "نفسه", "نفسها", "نفوس", "الانفس", "انفس",
            "نفيس", "تنفس", "نفسك", "نافس", "منافسة", "نفوسنا", "نفسا", "نفائس",
            "النفوس", "نفسهم"],
    # ---- lexicon batch 10 (ritual/legal/creedal roots; precision via indexing) ----
    "Slw": ["صلاة", "الصلاة", "صلوات", "الصلوات", "يصلي", "تصلي", "المصلي", "مصلي",
            "المصلين", "المصلون", "الصلوين", "صلاته", "صلاتهم", "صلاتك", "يصلون",
            "نصلي", "مصلون", "صلواتهم"],
    "Hjj": ["الحج", "حجة", "الحجة", "حجج", "حجيج", "الحجيج", "حجاج", "يحج",
            "حججت", "محجة", "حجوا", "بالحج", "للحج", "حاج", "حجك", "حجهم", "تحج"],
    "zkw": ["زكاة", "الزكاة", "زكا", "يزكي", "تزكي", "تزكى", "زكي", "زكية",
            "ازكى", "تزكية", "يزكو", "زكاتها", "الزكي", "زكوة", "زاك", "يزكون"],
    "Swm": ["صوم", "الصوم", "صيام", "الصيام", "صائم", "صائمين", "الصائمين", "يصوم",
            "تصوم", "صمت", "صوما", "صام", "صوموا", "صومه", "صيامكم", "الصائمات", "صياما"],
    "rkE": ["ركع", "الركوع", "ركوع", "راكع", "راكعين", "الراكعين", "يركع", "تركع",
            "اركعوا", "ركعا", "ركوعا", "راكعا", "يركعون", "ركعت", "الركع", "راكعون"],
    "Twf": ["طاف", "يطوف", "تطوف", "طواف", "الطواف", "طائف", "طوفان", "الطوفان",
            "طافوا", "يطوفون", "طوافا", "مطاف", "طائفة", "الطائفة", "طوائف", "طافت"],
    "sEy": ["سعى", "يسعى", "تسعى", "نسعى", "سعي", "السعي", "ساع", "الساعي",
            "مسعى", "سعت", "سعوا", "يسعون", "سعيا", "مساعي", "سعيه", "سعيها", "سعيكم"],
    "slm": ["سلام", "السلام", "سلم", "السلم", "اسلم", "يسلم", "تسلم", "مسلم",
            "مسلمين", "المسلمين", "اسلام", "الاسلام", "سالم", "سليم", "تسليم",
            "سلاما", "استسلم", "مسلمون", "سلمى", "يسلمون"],
    "$rk": ["شرك", "الشرك", "شريك", "شركاء", "مشرك", "مشركين", "المشركين", "يشرك",
            "تشرك", "اشرك", "شركا", "شركك", "اشركوا", "شركاءكم", "شركاؤهم",
            "شركائهم", "المشرك", "يشركون"],
    "dyn": ["دين", "الدين", "ديان", "يدين", "تدين", "دينه", "دينهم", "ديني",
            "دينك", "مدينين", "دائن", "ادان", "استدان", "الديان", "ديون", "تداين",
            "دينا", "يومالدين"],
    "nfq": ["انفق", "ينفق", "تنفق", "ننفق", "نفقة", "النفقة", "انفاق", "منفق",
            "المنافق", "منافق", "منافقين", "المنافقين", "نفاق", "النفاق", "نفق",
            "النفق", "انفقوا", "ينفقون", "تنفقون"],
    "gyb": ["غيب", "الغيب", "غائب", "الغائب", "غاب", "يغيب", "تغيب", "غيبة",
            "غياب", "غيابة", "غيبه", "غيوب", "الغيوب", "غيابت", "يغيبون", "غابوا",
            "مغيب", "الغائبين", "غيبا"],
    "qwm": ["قام", "يقوم", "تقوم", "اقام", "يقيم", "قائم", "قيام", "القيامة",
            "مقام", "قوم", "القوم", "اقوام", "مستقيم", "قيم", "القيم", "يقيمون",
            "اقاموا", "قوام", "قائمين", "استقام", "اقامة", "مقيم", "قومه", "قومي"],
    "$fE": ["شفع", "الشفاعة", "شفاعة", "شفيع", "الشفيع", "شفعاء", "يشفع", "تشفع",
            "شافع", "مشفع", "شفعت", "يشفعون", "شفعه", "استشفع", "الشافعين",
            "شفاعتهم", "الشفع", "شفعا", "شفعاؤنا"],
    # ---- scaling batch 2 (recall-first; precision supplied during indexing) ----
    "Hlm": ["حلم", "الحلم", "حليم", "حلماء", "تحلم", "يحلم", "احلام",
            "الاحلام", "حلوم", "محلم", "حلمي"],
    "jwd": ["جود", "الجود", "جواد", "اجواد", "الاجواد", "يجود", "تجود",
            "اجود", "مجود", "جدا", "جدوى"],
    "ErD": ["عرض", "عرضي", "عرضه", "عرضك", "اعراض", "الاعراض", "عراض",
            "معرض", "تعرض", "عارض", "عروض", "معروض"],
    "mwt": ["موت", "الموت", "مات", "ماتوا", "يموت", "تموت", "نموت", "ميت",
            "ميتة", "اموات", "الموتى", "مميت", "يميت", "استمات", "مستميت"],
    "$kr": ["شكر", "الشكر", "شاكر", "شكور", "تشكر", "يشكر", "نشكر", "اشكر",
            "مشكور", "شكرت", "شاكرين", "شكرا"],
    "Sbr": ["صبر", "الصبر", "صابر", "صبور", "اصبر", "يصبر", "تصبر", "نصبر",
            "صبرت", "صبرا", "الصابرين", "اصطبر", "صبار", "تصبروا"],
    "Ebd": ["عبد", "العبد", "عبيد", "عباد", "العباد", "يعبد", "تعبد", "نعبد",
            "اعبد", "معبد", "عبادة", "عبدت", "عابد", "استعبد", "عبدا"],
    "Zlm": ["ظلم", "الظلم", "ظالم", "مظلوم", "ظلام", "الظلام", "اظلم", "يظلم",
            "تظلم", "ظلوم", "ظلمات", "ظلماء", "الظالمين", "ظلمت"],
    "Hqq": ["الحق", "حق", "حقا", "حقوق", "الحقوق", "حقيق", "الحقيقة", "محق",
            "يحق", "تحق", "استحق", "احق", "حققت", "حقيقة"],
    # batch 11 — Wave-2 scriptural / legal / creedal / worship / ethical roots
    "wHy": ["وحي", "الوحي", "اوحى", "اوحي", "يوحي", "نوحي", "توحي", "وحيا",
            "موحى", "اوحينا", "اوحيت"],
    "rsl": ["رسول", "الرسول", "رسل", "الرسل", "رسالة", "رسالات", "ارسل", "ارسلنا",
            "ارسلت", "يرسل", "نرسل", "ترسل", "مرسل", "مرسلون", "مرسلين", "رسلنا",
            "رسله", "رسولا"],
    "nbA": ["نبا", "النبا", "انباء", "نبأ", "انبأ", "ينبئ", "ننبئ", "نبئ", "نبوة",
            "نبي", "النبي", "انبياء", "الانبياء", "نبيا"],
    "ktb": ["كتب", "الكتاب", "كتاب", "الكتب", "كاتب", "مكتوب", "يكتب", "نكتب",
            "تكتب", "اكتب", "كتابا", "كتيبة", "الكتائب", "كتائب", "كتبت"],
    "qrA": ["قرا", "القران", "قران", "يقرا", "تقرا", "اقرا", "قراءة", "قارئ",
            "قروء", "قرء", "مقروء"],
    "frq": ["فرق", "الفرقان", "فرقان", "فريق", "فرقة", "افترق", "تفرق", "يفرق",
            "نفرق", "فارق", "مفارق", "فراق", "فروق", "فرقا", "تفريق"],
    "fsq": ["فسق", "الفسق", "فاسق", "فاسقون", "فاسقين", "يفسق", "تفسق", "فسوق",
            "فسقة", "فاسقا"],
    "Hll": ["حل", "الحلال", "حلال", "حلت", "يحل", "تحل", "احل", "محل", "محلة",
            "حلول", "حلائل", "مستحل", "الحل", "حالا"],
    "jhd": ["جهد", "الجهاد", "جهاد", "جاهد", "مجاهد", "يجاهد", "نجاهد", "جاهدوا",
            "اجتهد", "جهيد", "مجهود", "جهدا"],
    "brk": ["برك", "البركة", "بركة", "مبارك", "تبارك", "يبارك", "بارك", "بروك",
            "براك", "مبرك", "بركات", "باركنا", "بركت"],
    "Tlq": ["طلق", "طلاق", "طالق", "مطلقة", "طلقت", "اطلق", "ينطلق", "انطلق",
            "طليق", "مطلق", "طوالق", "انطلقوا", "منطلق"],
    "Hnf": ["حنف", "حنيف", "حنفاء", "الحنيفية", "تحنف", "احنف", "حنيفا"],
    "Hrb": ["حرب", "الحرب", "حروب", "حارب", "محارب", "يحارب", "حربة", "محراب",
            "حريب", "حربا", "الحروب"],
    "qds": ["قدس", "القدس", "قدوس", "مقدس", "المقدسة", "تقدس", "يقدس", "قدسي",
            "الاقداس", "المقدس"],
    "nsk": ["نسك", "مناسك", "منسك", "ناسك", "نسيكة", "نسائك", "ينسك", "منسكا",
            "نسكي"],
    "wly": ["ولي", "الولي", "اولياء", "مولى", "موالي", "ولاية", "تولى", "يتولى",
            "اولى", "والي", "وال", "يلي", "الموالي", "موليا", "ولوا"],
    "sbH": ["سبح", "التسبيح", "تسبيح", "سبحان", "يسبح", "نسبح", "سبحت", "سابح",
            "سوابح", "سباحة", "مسبح", "سبحة", "يسبحون"],
    "Eml": ["عمل", "العمل", "اعمال", "عامل", "يعمل", "نعمل", "تعمل", "اعمل",
            "عملوا", "معمول", "عملة", "اعملوا", "عملك", "عاملة", "عملا"],
    # batch 12 — Wave-4 path / law / command / eschatology / psychology / recompense roots
    "$rE": ["شرع", "شريعة", "الشريعة", "شرعة", "مشرع", "شارع", "شوارع", "يشرع",
            "شرعا", "شرائع", "شرعت"],
    "fTr": ["فطر", "فطرة", "الفطرة", "فاطر", "افطر", "ينفطر", "تفطر", "مفطور",
            "انفطرت", "فطور", "فطرت"],
    "twb": ["تاب", "التوبة", "توبة", "يتوب", "نتوب", "تواب", "تائب", "متاب",
            "توابون", "تبت", "تابوا"],
    "sbl": ["سبيل", "السبيل", "سبل", "السبل", "سابلة", "مسبل", "اسبل", "سلسبيل",
            "سبيلا", "اسبلت", "سبول"],
    "SrT": ["صراط", "الصراط", "صراطا", "صرط"],
    "Amr": ["امر", "الامر", "امور", "اوامر", "يامر", "نامر", "امير", "الامير",
            "امارة", "ائتمر", "امرا", "امراء", "اميرا"],
    "nhy": ["نهى", "ينهى", "تنهى", "نهي", "النهى", "منتهى", "نهاية", "انتهى",
            "نهية", "النهي", "ينتهي", "نهيت"],
    "bEv": ["بعث", "يبعث", "نبعث", "بعثت", "مبعوث", "انبعث", "باعث", "بعيث",
            "بعثا", "بعثوا", "ينبعث"],
    "H$r": ["حشر", "يحشر", "نحشر", "محشر", "حشرت", "الحشر", "حاشر", "محشور",
            "حشرا", "حشروا"],
    "rDw": ["رضا", "الرضا", "رضي", "يرضى", "ترضى", "راض", "مرضي", "رضوان",
            "مرضاة", "رضوا", "ارضى", "رضيت", "راضية"],
    "Hsn": ["حسن", "الحسن", "حسناء", "احسن", "محسن", "يحسن", "تحسن", "حسان",
            "احسان", "حسنة", "محاسن", "حسنا", "حسنات", "الحسنى"],
    "flH": ["فلح", "افلح", "مفلح", "الفلاح", "فلاح", "يفلح", "تفلح", "فلاحة",
            "مفلحون", "افلحوا"],
    "hwy": ["هوى", "الهوى", "اهواء", "يهوي", "تهوي", "هاو", "هاوية", "مهوى",
            "هوة", "اهوى", "هويت", "اهوائهم"],
    "rwH": ["روح", "الريح", "ريح", "رياح", "راح", "يروح", "رواح", "مراح",
            "ريحان", "ارواح", "استراح", "راحة", "مرتاح", "اريحية"],
    "qlb": ["قلب", "القلب", "قلوب", "يقلب", "تقلب", "انقلب", "مقلوب", "متقلب",
            "قلبا", "تتقلب", "قلبت"],
    "Sdr": ["صدر", "الصدر", "صدور", "تصدر", "يصدر", "مصدر", "اصدر", "صادر",
            "صدرا", "صدورهم", "صدرت"],
    "Eql": ["عقل", "العقل", "عقول", "يعقل", "تعقل", "عاقل", "معقول", "عقال",
            "اعتقل", "عقيلة", "معقل", "عقلا", "يعقلون"],
    "fkr": ["فكر", "تفكر", "يتفكر", "فكرة", "مفكر", "تفكير", "افتكر", "يتفكرون",
            "فكرت"],
    "nfE": ["نفع", "ينفع", "تنفع", "منفعة", "منافع", "نافع", "انفع", "نفعا",
            "ينتفع", "نفعت"],
    "Drr": ["ضر", "الضر", "ضرر", "اضر", "يضر", "تضر", "مضرة", "ضرير", "ضراء",
            "مضطر", "ضرا", "ضرة", "الضراء"],
    "xsr": ["خسر", "خسران", "خاسر", "يخسر", "تخسر", "خسارة", "اخسر", "خواسر",
            "خاسرين", "خسروا"],
    "gfl": ["غفل", "غافل", "يغفل", "تغفل", "غفلة", "اغفل", "مغفل", "غفلا",
            "غافلون", "غفلت"],
    "byn": ["بين", "البين", "بان", "يبين", "تبين", "بيان", "بينة", "مبين",
            "تبيان", "بينات", "ابان", "بينا", "تبيين", "بينهم"],
    "swy": ["سوى", "سواء", "استوى", "يستوي", "تستوي", "مستوي", "سوي", "تسوية",
            "سوية", "سوايا", "مستوية", "استوت"],
    "Ajr": ["اجر", "الاجر", "اجور", "اجير", "ياجر", "استاجر", "ماجور", "اجرا",
            "اجرة", "اجورهم", "اجران"],
    "jzy": ["جزى", "يجزي", "تجزي", "جزاء", "الجزاء", "مجزي", "جزية", "تجازي",
            "جزاه", "جزوا", "جزيت"],
    "wzn": ["وزن", "الميزان", "ميزان", "يزن", "توزن", "موزون", "وزين", "اوزان",
            "موازين", "وزنا", "وزنت"],
    "ksb": ["كسب", "يكسب", "تكسب", "كاسب", "مكتسب", "اكتسب", "كسبت", "مكسب",
            "كسبا", "كسبوا", "يكتسب"],
    "hlk": ["هلك", "يهلك", "تهلك", "هالك", "مهلك", "اهلك", "هلاك", "تهلكة",
            "مهالك", "هوالك", "هلكى", "هلكوا"],
    "njw": ["نجا", "نجاة", "ينجو", "تنجو", "ناج", "منجى", "انجى", "نجوى",
            "نجي", "نجاء", "ناجية", "نجوت", "نجاه"],
    "$yE": ["شيعة", "شيع", "اشياع", "يشيع", "مشيع", "شائع", "تشييع", "مشياع",
            "شيعا", "شيعته"],
    "gDb": ["غضب", "الغضب", "غاضب", "يغضب", "تغضب", "مغضوب", "غضبان", "غضبة",
            "اغضب", "غضبت"],
    "slT": ["سلطان", "السلطان", "سلطة", "مسلط", "تسلط", "يسلط", "سليط", "سلاطة",
            "سلطانا", "تسليط"],
    "Sfw": ["صفا", "الصفا", "صفو", "صفوة", "اصطفى", "يصطفي", "صاف", "مصطفى",
            "صفي", "صفية", "اصفياء", "صفوان", "صافية"],
    "vwb": ["ثوب", "الثواب", "ثواب", "ثياب", "اثاب", "يثيب", "مثوبة", "مثابة",
            "ثائب", "ثوبا", "اثابهم"],
    "Edw": ["عدو", "العدو", "اعداء", "عادى", "يعدو", "تعدو", "عدوان", "عادية",
            "معتد", "تعدى", "اعتدى", "عدوا", "عدى", "العدوان"],
    "brr": ["بر", "البر", "ابرار", "بار", "يبر", "تبر", "بريرة", "مبرور",
            "برا", "بارة", "بررة"],
    "frH": ["فرح", "يفرح", "تفرح", "فرحان", "مفروح", "افراح", "فرحة", "فرحا",
            "فرحوا", "فرحت"],
    "Hzn": ["حزن", "الحزن", "حزين", "يحزن", "تحزن", "احزن", "محزون", "احزان",
            "حزنا", "حزنت", "تحزنوا"],
    # batch 13 — Wave-5 guidance / error / command-prohibition / decree / oath roots
    "nzl": ["نزل", "ينزل", "تنزل", "نزول", "منزل", "منزلة", "انزل", "تنزيل",
            "نزلت", "نزال", "منازل", "نزيل", "نزلوا", "منزلا"],
    "Dll": ["ضلال", "الضلال", "ضالة", "يضل", "تضل", "اضل", "ضليل", "الضليل",
            "ضلالة", "ضلت", "ضلوا", "مضل", "مضلل", "ضلالا"],
    "bgy": ["بغى", "يبغي", "تبغي", "بغي", "باغ", "بغاء", "ابتغى", "مبتغى",
            "بغية", "بغوا", "يبتغي", "ينبغي", "بغيا"],
    "zwj": ["زوج", "الزوج", "ازواج", "زوجة", "تزوج", "يزوج", "مزوج", "زوجت",
            "ازدوج", "زوجين", "زوجا"],
    "ymn": ["يمين", "اليمين", "ايمان", "يمن", "ميمون", "يمنى", "الايمن", "يمينا",
            "اليمن", "ايمانهم"],
    "Erf": ["عرف", "يعرف", "تعرف", "معروف", "عارف", "عرفان", "اعترف", "تعارف",
            "العرف", "معرفة", "عريف", "اعرف", "معروفة", "عرفت"],
    "qDy": ["قضى", "يقضي", "تقضي", "قضاء", "قاض", "مقضي", "انقضى", "قضية",
            "قضت", "قضوا", "مقتضى", "قضاها"],
    "mkr": ["مكر", "يمكر", "تمكر", "ماكر", "مكور", "ممكور", "مكرت", "مكروا",
            "مكار", "ممكورة"],
    "Tgy": ["طغى", "يطغى", "تطغى", "طاغ", "طغيان", "طاغوت", "طاغية", "طغوا",
            "طغت", "طاغين"],
    "nkr": ["نكر", "ينكر", "تنكر", "منكر", "نكير", "نكراء", "انكر", "استنكر",
            "نكرة", "منكرا", "نكرت"],
    "wrv": ["ورث", "يرث", "نرث", "وارث", "ميراث", "ارث", "وراثة", "ورثة",
            "موروث", "تراث", "ورثوا"],
    "kyd": ["كيد", "يكيد", "تكيد", "كائد", "مكيدة", "كياد", "كيدا", "كيده",
            "كيدهم"],
    "qsm": ["قسم", "يقسم", "تقسم", "قسمة", "اقسم", "قسيم", "مقسم", "قسام",
            "اقسام", "مقسوم", "قسما", "قاسم", "تقاسم", "اقساما"],
    "hjr": ["هجر", "يهجر", "تهجر", "هجران", "هاجر", "مهاجر", "هجرة", "مهجور",
            "هجرت", "هواجر", "الهجير", "هجرا", "الهاجرة"],
    "jhl": ["جهل", "يجهل", "تجهل", "جاهل", "جهول", "جهالة", "جهلاء", "تجاهل",
            "مجهول", "جهلا", "الجاهلية", "جهلت"],
    "srf": ["سرف", "اسراف", "مسرف", "يسرف", "تسرف", "سرفت", "مسرفون", "اسرفوا",
            "سرفا", "الاسراف"],
    "gwy": ["غوى", "يغوي", "تغوي", "غي", "غاو", "غواية", "اغوى", "غوت", "غووا",
            "الغي", "غيا", "غاوين"],
    "Hzb": ["حزب", "الاحزاب", "احزاب", "حزبه", "تحزب", "حزبت", "حزبا", "حزبهم"],
    "r$d": ["رشد", "يرشد", "ترشد", "راشد", "رشيد", "رشاد", "ارشد", "مرشد",
            "رشدا", "الرشد", "رشده"],
    "frD": ["فرض", "يفرض", "تفرض", "فريضة", "مفروض", "افترض", "فرائض", "فرضت",
            "فرضا", "مفترض", "فرضنا"],
    "fdy": ["فدى", "يفدي", "تفدي", "فداء", "فدية", "افتدى", "مفدي", "فديت",
            "فداك", "فدوه", "فداها"],
    "Hlf": ["حلف", "يحلف", "تحلف", "حليف", "حلف", "محالفة", "تحالف", "حلفاء",
            "حلفت", "حلوف", "احلاف"],
    "rhb": ["رهب", "يرهب", "ترهب", "راهب", "رهبة", "رهبان", "رهبانية", "ارهب",
            "رهبا", "مرهوب", "الرهبان"],
    "sfh": ["سفه", "يسفه", "تسفه", "سفيه", "سفهاء", "سفاهة", "متسفه", "سفها",
            "سفهت"],
    "rgb": ["رغب", "يرغب", "ترغب", "راغب", "رغبة", "رغائب", "رغبا", "راغبون",
            "رغبت", "رغيب"],
    "wsT": ["وسط", "الوسط", "اوسط", "وسطى", "توسط", "متوسط", "وساط", "اوساط",
            "الاوسط", "وسيط", "وسطا"],
    # batch 14 — Wave-6 high-frequency theological / eschatological / cosmological roots
    "Alh": ["الله", "اله", "الاله", "الهة", "الالهة", "اللهم", "الهي", "الها",
            "تالله", "بالله"],
    "Ayy": ["اية", "الاية", "ايات", "الايات", "ايه", "اياته", "اياتنا", "ايتها"],
    "smw": ["سماء", "السماء", "سماوات", "سموات", "اسم", "الاسم", "اسماء", "سما",
            "يسمو", "سامي", "سمي", "سماة", "سامق"],
    "E*b": ["عذاب", "العذاب", "يعذب", "تعذب", "معذب", "عذب", "عذبة", "تعذيب",
            "عذابا", "عذابهم", "عذوبة"],
    "qbl": ["قبل", "القبلة", "قبلة", "يقبل", "تقبل", "اقبل", "مقبل", "قابل",
            "استقبل", "قبيل", "قبائل", "قبلا", "مقابل"],
    "*kr": ["ذكر", "الذكر", "يذكر", "تذكر", "ذاكر", "مذكور", "ذكرى", "تذكرة",
            "ذكور", "اذكر", "ذكرت", "ذكرا", "مذكر"],
    "k*b": ["كذب", "الكذب", "يكذب", "تكذب", "كاذب", "كذاب", "مكذب", "كذبة",
            "كذوب", "كذبوا", "تكذيب", "كذبا"],
    "Axr": ["اخر", "الاخر", "اخرى", "اخرة", "الاخرة", "اخرون", "اخير", "تاخر",
            "اواخر", "اخرين", "اخرا", "مؤخر"],
    "nws": ["ناس", "الناس", "اناس", "اناسي", "نوس", "ينوس", "ناسوت", "نواس"],
    "xyr": ["خير", "الخير", "خيار", "خيرة", "اختار", "يختار", "مختار", "خيرا",
            "اخيار", "تخير", "خيور", "خيرات"],
    "SlH": ["صلح", "الصلح", "صالح", "اصلح", "يصلح", "تصلح", "صلاح", "مصلح",
            "صالحة", "اصلاح", "صلحا", "صالحون"],
    "qtl": ["قتل", "القتل", "يقتل", "تقتل", "قاتل", "مقتول", "قتال", "مقتل",
            "قتلى", "قتيل", "قتلوا", "مقاتل", "اقتتل", "قتلت"],
    "mvl": ["مثل", "المثل", "امثال", "مثال", "تمثل", "يمثل", "مثيل", "مثلا",
            "امثلة", "تمثال", "ممثل", "مثلت"],
    "n*r": ["نذر", "ينذر", "تنذر", "نذير", "منذر", "انذر", "نذور", "نذرت",
            "انذار", "نذرا", "منذرين"],
    "TwE": ["طوع", "طاعة", "اطاع", "يطيع", "تطيع", "مطيع", "مطاع", "تطوع",
            "طائع", "طوعا", "استطاع", "مطواع"],
    "wdd": ["الود", "مودة", "المودة", "يود", "وداد", "ودود", "وديد", "ودت",
            "توادد", "ودا", "اوددت"],
    "klm": ["كلام", "الكلام", "كلمة", "الكلمة", "كلمات", "يكلم", "تكلم", "كليم",
            "متكلم", "تكليم", "كلمه", "مكالمة"],
    "$wr": ["شور", "شورى", "اشار", "يشير", "تشير", "مشير", "مشورة", "استشار",
            "اشارة", "مشاورة", "شوار", "مشوار"],
    "EZm": ["عظم", "العظم", "عظيم", "اعظم", "عظمة", "عظام", "تعظيم", "عظما",
            "معظم", "عظيمة", "عظمت"],
    "$yA": ["شيء", "الشيء", "اشياء", "شاء", "يشاء", "تشاء", "نشاء", "مشيئة",
            "شيئا", "شئت", "شاءت"],
    "ArD": ["ارض", "الارض", "اراض", "اراضي", "ارضون", "ارضين", "ارضا", "الارضين"],
    "ywm": ["يوم", "اليوم", "ايام", "يومئذ", "يوما", "الايام", "يومكم", "يومهم",
            "يومها"],
    "rAy": ["راى", "يرى", "ترى", "راي", "الراي", "رؤية", "رؤيا", "ارى", "نرى",
            "مرئي", "رايت", "ارايت"],
    "tbE": ["تبع", "يتبع", "تتبع", "اتبع", "متبع", "تابع", "اتباع", "تبعا",
            "تبعت", "تباعا", "متتابع"],
    "jmE": ["جمع", "الجمع", "يجمع", "تجمع", "جامع", "مجموع", "جماعة", "جميع",
            "اجمع", "مجمع", "جموع", "جمعا"],
    "$ms": ["شمس", "الشمس", "شموس", "شامس", "شميس", "شمسا", "مشمس"],
    "qmr": ["قمر", "القمر", "اقمار", "قمراء", "اقمر", "مقمر", "قمرا", "قمير"],
    "njm": ["نجم", "النجم", "نجوم", "انجم", "نجمة", "نجما", "منجم", "النجوم"],
    # batch 15 — Wave-7 final load-bearing roots (will / encounter / sin / justice / seal / remainder)
    "Awl": ["أول", "الأول", "أولى", "أوائل", "أولون", "مآل", "تأويل", "آل",
            "أولات", "أولي", "أولا"],
    "kvr": ["كثر", "الكثير", "كثير", "كثرة", "اكثر", "يكثر", "كثار", "كوثر",
            "مكاثرة", "كثيرا", "كثروا"],
    "gyr": ["الغير", "غيره", "غيرها", "تغير", "يتغير", "تغيير", "غيرة", "اغار",
            "مغيرة", "غيار", "مغار", "غيور"],
    "rwd": ["اراد", "يريد", "تريد", "ارادة", "مريد", "رائد", "رويد", "ارتاد",
            "مرتاد", "رواد", "اراده"],
    "lqy": ["لقي", "يلقى", "تلقى", "لقاء", "ملاق", "القى", "يلقي", "تلقاء",
            "ملقى", "تلاقى", "لقيت", "ملتقى"],
    "dnw": ["دنا", "يدنو", "تدنو", "ادنى", "الدنيا", "دنيا", "داني", "دنو",
            "دنية", "دانية", "ادنيت"],
    "nZr": ["نظر", "ينظر", "تنظر", "ناظر", "منظر", "نظرة", "انتظر", "نظير",
            "نظار", "مناظر", "انظر", "نظرت"],
    "sAl": ["سأل", "يسأل", "تسأل", "سؤال", "سائل", "مسألة", "مسؤول", "تساءل",
            "سؤل", "سألت", "سائلة"],
    "fEl": ["فعل", "يفعل", "تفعل", "فاعل", "مفعول", "فعال", "فعلة", "افعال",
            "فعلت", "فعلوا", "افعل"],
    "*wq": ["ذاق", "يذوق", "تذوق", "ذوق", "مذاق", "اذاق", "ذواق", "ذقت",
            "ذاقوا", "مذيق", "تذوق"],
    "nsy": ["نسي", "ينسى", "تنسى", "نسيان", "منسي", "انسى", "تناسى", "نسيت",
            "نسوا", "منسيا", "نسيه"],
    "*nb": ["ذنب", "الذنب", "ذنوب", "مذنب", "اذناب", "ذنبا", "ذنبه", "ذنابى",
            "اذنبت", "مذنبين"],
    "jnH": ["جناح", "جنح", "يجنح", "تجنح", "جانح", "اجنحة", "جنوح", "جناحا",
            "جوانح", "جنحوا"],
    "wzr": ["وزر", "الوزر", "اوزار", "وزير", "وزراء", "وازر", "وزرا", "موزر",
            "اوزارها", "وزيرا"],
    "qsT": ["قسط", "القسط", "اقسط", "يقسط", "مقسط", "قاسط", "قسطاس", "اقساط",
            "قسطا", "مقسطين"],
    "lbs": ["لبس", "اللباس", "لباس", "ملبس", "يلبس", "تلبس", "لابس", "لبوس",
            "التبس", "لبست", "ملابس"],
    "tmm": ["التمام", "تمام", "اتم", "يتم", "تتم", "متم", "تمت", "تماما",
            "اتمام", "تميمة", "تمموا"],
    "bqy": ["بقي", "يبقى", "تبقى", "باق", "باقية", "ابقى", "بقية", "بقاء",
            "بقيت", "بقوا", "تبقي"],
    "xtm": ["ختم", "يختم", "تختم", "خاتم", "مختوم", "ختام", "خاتمة", "ختمت",
            "خواتم", "ختاما"],
    "bdE": ["بدع", "بديع", "مبدع", "ابتدع", "بدعة", "مبتدع", "ابدع", "يبدع",
            "بدعا", "بديعة"],
    "DyE": ["ضيع", "يضيع", "تضيع", "ضائع", "مضيع", "ضياع", "اضاع", "ضيعة",
            "ضيعت", "اضاعوا", "تضييع"],
    "jnd": ["جند", "الجند", "جنود", "اجناد", "جندي", "جندا", "جنوده", "مجندة",
            "جنودهم"],
    # batch 16 — Wave-8 remaining load-bearing theological / anthropological roots
    "Amm": ["أمة", "الأمة", "أمم", "إمام", "الإمام", "أئمة", "أمهات", "أموم",
            "أمامه", "امة", "امام", "أمما"],
    "b$r": ["بشر", "البشر", "بشرى", "بشير", "مبشر", "يبشر", "تبشر", "بشارة",
            "بشرة", "بشرا", "ابشر", "استبشر", "بشريات"],
    "xlf": ["خلف", "الخلف", "خليفة", "خلائف", "خلفاء", "يخلف", "تخلف", "اختلف",
            "خلاف", "مختلف", "استخلف", "خلفه", "خوالف"],
    "fDl": ["فضل", "الفضل", "فضائل", "افضل", "يفضل", "تفضل", "فاضل", "مفضل",
            "فضلة", "تفضيل", "فضلا", "فواضل"],
    "A*n": ["أذن", "الأذن", "آذان", "إذن", "يأذن", "تأذن", "استأذن", "مؤذن",
            "أذان", "مأذون", "اذن", "اذان"],
    "Ans": ["إنس", "الإنس", "إنسان", "الإنسان", "أنس", "أنيس", "آنس", "مؤنس",
            "إنسي", "أناسي", "استأنس", "انسان"],
    "Hbb": ["الحب", "حبيب", "محب", "محبوب", "يحب", "تحب", "احب", "محبة",
            "احباب", "حبيبة", "حبائب", "حببت"],
    "$Tn": ["شيطان", "الشيطان", "شياطين", "شطن", "شاطن", "شطون", "شطنا",
            "شياطينه"],
    "Eqb": ["عقب", "العقب", "عاقبة", "العاقبة", "عقاب", "يعاقب", "تعاقب", "معقب",
            "اعقاب", "عقبى", "عقبة", "عواقب", "عقيب"],
    "rjE": ["رجع", "يرجع", "ترجع", "راجع", "مرجع", "رجعة", "رجوع", "ارجع",
            "تراجع", "رجعى", "رواجع", "رجعا", "مرجعهم"],
    "mwl": ["المال", "اموال", "اموالهم", "اموالكم", "اموالنا", "مالك", "ماله",
            "مالي", "تمول", "مولا"],
    "SHb": ["صحب", "الصحب", "صاحب", "اصحاب", "صحبة", "يصحب", "تصحب", "مصاحب",
            "صواحب", "صحابة", "اصحابه", "صاحبة"],
    "wld": ["ولد", "الولد", "اولاد", "يلد", "تلد", "والد", "مولود", "ولادة",
            "ولدت", "والدة", "والدين", "ولدان", "مولد"],
    # batch 17 — Wave-9 final content roots (evil / conveying / face / might / pain / calamity)
    "swA": ["سوء", "السوء", "سيئة", "سيئات", "اساء", "يسيء", "مسيء", "سوءة",
            "سوءى", "اسوا", "سوءته", "اساءت"],
    "blg": ["بلغ", "يبلغ", "تبلغ", "بالغ", "مبلغ", "بلاغ", "بلوغ", "ابلغ",
            "تبليغ", "بليغ", "بلغة", "بلغت"],
    "wjh": ["وجه", "الوجه", "وجوه", "يوجه", "توجه", "جهة", "وجهة", "واجه",
            "مواجهة", "اوجه", "وجهك", "جاه", "الوجوه"],
    "bAs": ["باس", "البأس", "بأساء", "بئيس", "بائس", "بؤس", "بئس", "بأسهم",
            "بأسا", "بئيسا", "باسل"],
    "Alm": ["اليم", "الاليم", "اليمة", "اليما", "مؤلم", "يؤلم", "تألم", "الاما",
            "اليمٍ", "مؤلمة"],
    "Swb": ["صاب", "اصاب", "يصيب", "تصيب", "مصيب", "مصيبة", "صواب", "صوب",
            "صيب", "مصاب", "مصائب", "صائب", "صوبا"],
    "Akl": ["اكل", "الاكل", "ياكل", "تاكل", "اكول", "ماكول", "اكلة", "ماكل",
            "اكلت", "مأكل", "أكل", "أكلت"],
    "dxl": ["دخل", "يدخل", "تدخل", "داخل", "مدخل", "دخول", "ادخل", "دخيل",
            "مداخل", "دخلت", "دخلوا"],
    "xrj": ["خرج", "يخرج", "تخرج", "خارج", "مخرج", "خروج", "اخرج", "خراج",
            "مخارج", "خرجت", "خرجوا", "استخرج"],
    "Ahl": ["اهل", "الاهل", "اهلون", "اهلين", "اهالي", "اهلا", "اهله", "اهلها",
            "مأهول", "تأهل"],
    "wjd": ["وجد", "يجد", "تجد", "واجد", "موجود", "وجود", "اوجد", "وجدان",
            "وجدت", "وجدوا", "مواجيد"],
    "$dd": ["الشدة", "شديد", "اشد", "يشد", "مشدد", "شداد", "شدائد", "اشتد",
            "شدة", "اشداء", "شديدة"],
    # batch 18 — Wave-10 second-tier load-bearing content roots (epistemic / ethical / eschatological)
    "Znn": ["ظن", "الظن", "يظن", "تظن", "ظنون", "ظنين", "مظنون", "ظننت",
            "ظنوا", "اظن", "ظنا", "ظنة"],
    "jrm": ["جرم", "مجرم", "اجرم", "يجرم", "جريمة", "جرائم", "اجرام", "مجرمون",
            "لاجرم", "جرمت", "مجرمين"],
    "Hml": ["حمل", "يحمل", "تحمل", "حامل", "محمول", "حمولة", "احمال", "حمالة",
            "حملت", "حوامل", "محمل", "حملوا"],
    "ftn": ["فتن", "فتنة", "يفتن", "تفتن", "فاتن", "مفتون", "افتتن", "فتون",
            "فتان", "فتنت", "فتنوا"],
    "fsd": ["فسد", "فساد", "يفسد", "تفسد", "مفسد", "افسد", "فاسد", "مفسدة",
            "فسدت", "مفسدون", "فسادا"],
    "x$y": ["خشي", "يخشى", "تخشى", "خشية", "خاش", "خشيان", "اخشى", "خشيت",
            "خشوا", "مخشي", "خشية"],
    "Avm": ["اثم", "الاثم", "اثيم", "اثام", "ماثم", "تأثم", "اثما", "آثام",
            "أثيم", "مأثم"],
    "sxr": ["سخر", "يسخر", "تسخر", "ساخر", "مسخر", "سخرية", "سخري", "استسخر",
            "تسخير", "سخروا", "مسخرة"],
    "lEn": ["لعن", "اللعن", "يلعن", "تلعن", "لاعن", "ملعون", "لعنة", "لعين",
            "لعنوا", "لعنت", "لعائن"],
    "Tyb": ["طيب", "الطيب", "طيبة", "طيبات", "اطيب", "يطيب", "طاب", "طوبى",
            "طيبا", "اطايب", "استطاب"],
    "fry": ["فرى", "يفري", "افترى", "مفتر", "فرية", "افتراء", "مفترى", "فريت",
            "افتروا", "افتريت"],
    "mtE": ["متع", "المتاع", "متاع", "يمتع", "تمتع", "ممتع", "امتعة", "متعة",
            "استمتع", "تمتيع", "متعت"],
    "skn": ["سكن", "يسكن", "تسكن", "ساكن", "مسكن", "سكينة", "سكون", "اسكن",
            "مساكن", "سكان", "مسكين", "مساكين"],
    "fSl": ["فصل", "يفصل", "تفصل", "فاصل", "مفصل", "تفصيل", "فصيل", "انفصل",
            "فصلت", "فيصل", "فصائل"],
    "Zhr": ["ظهر", "الظهر", "يظهر", "تظهر", "ظاهر", "مظهر", "ظهور", "اظهر",
            "تظاهر", "ظهير", "ظهرت", "ظواهر"],
    "DEf": ["ضعف", "الضعف", "ضعيف", "يضعف", "تضعف", "اضعاف", "مضاعف", "استضعف",
            "ضعفاء", "ضعفت", "ضعفا"],
    "qwy": ["قوة", "القوة", "قوي", "اقوى", "قوى", "مقو", "قوية", "قويت",
            "قواء", "قويا"],
    "xbr": ["خبر", "الخبر", "اخبار", "خبير", "يخبر", "تخبر", "مخبر", "اختبر",
            "خبرة", "استخبر", "خبرت", "مخابر"],
    "TEm": ["طعم", "الطعام", "طعام", "يطعم", "تطعم", "طاعم", "مطعم", "اطعم",
            "طعمة", "استطعم", "اطعام", "مطاعم"],
    "zyd": ["زاد", "يزيد", "تزيد", "زائد", "مزيد", "زيادة", "ازداد", "استزاد",
            "زدت", "زادوا", "تزايد"],
    "drk": ["درك", "يدرك", "تدرك", "مدرك", "ادرك", "تدارك", "ادراك", "مدارك",
            "دركات", "ادركت", "استدرك"],
    "Drb": ["ضرب", "يضرب", "تضرب", "ضارب", "مضروب", "اضرب", "مضرب", "ضربة",
            "ضراب", "مضارب", "ضربت", "ضربوا"],
}

JAHILI_ERA_VALUES = {
    "جاهلي", "الجاهلي", "العصر الجاهلي", "ما قبل الاسلام", "ماقبلالاسلام",
    "pre-islamic", "preislamic", "jahili", "jahiliyya", "al-jahili",
}

# Candidate CSV header names (Arabic + English) for auto-detection.
COL_CANDIDATES = {
    "era":   ["العصر", "poet_era", "era", "age", "period", "epoch"],
    "poet":  ["الشاعر", "اسم الشاعر", "poet_name", "poet", "author", "name"],
    "text":  ["القصيدة", "الابيات", "الأبيات", "البيت", "النص", "الشعر",
              "poem_text", "poem", "text", "poetry", "verse", "bayt", "content"],
    "title": ["عنوان القصيدة", "العنوان", "poem_title", "title", "subject"],
    "tags":  ["poem_tags", "tags", "الوسوم", "poem_tag", "tag"],
    "meter": ["البحر", "بحر", "meter", "bahr"],
    "rhyme": ["القافية", "قافية", "rhyme", "qafia", "qafiya"],
}

TASHKEEL_RE = re.compile(r"[ؐ-ًؚ-ٰٟۖ-ۭـ]")


# ------------------------------------------------------------------------
# DB helpers
# ------------------------------------------------------------------------

def get_conn() -> sqlite3.Connection:
    conn = sqlite3.connect(DB)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA foreign_keys = ON")
    return conn


def ensure_schema(conn: sqlite3.Connection) -> None:
    """Idempotently create the three corpus tables + index."""
    conn.executescript(
        """
        CREATE TABLE IF NOT EXISTS poetry_poems (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            poet        TEXT,
            poet_latin  TEXT,
            era         TEXT,
            title       TEXT,
            meter       TEXT,
            rhyme       TEXT,
            tags        TEXT,
            auth_tier   TEXT NOT NULL DEFAULT 'C',
            source      TEXT,
            source_ref  TEXT,
            full_text   TEXT,
            notes       TEXT,
            created_at  TEXT DEFAULT CURRENT_TIMESTAMP
        );

        CREATE TABLE IF NOT EXISTS poetry_lines (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            poem_id     INTEGER NOT NULL REFERENCES poetry_poems(id),
            line_no     INTEGER,
            hemistich1  TEXT,
            hemistich2  TEXT,
            text_plain  TEXT,
            UNIQUE(poem_id, line_no)
        );

        CREATE TABLE IF NOT EXISTS poetry_line_roots (
            id              INTEGER PRIMARY KEY AUTOINCREMENT,
            line_id         INTEGER NOT NULL REFERENCES poetry_lines(id),
            root_buckwalter TEXT NOT NULL,
            root_arabic     TEXT,
            surface_word    TEXT,
            sense_hint      TEXT,
            extractor_model TEXT,
            confidence      REAL,
            verified        INTEGER DEFAULT 0,
            created_at      TEXT DEFAULT CURRENT_TIMESTAMP,
            UNIQUE(line_id, root_buckwalter)
        );
        CREATE INDEX IF NOT EXISTS idx_poetry_line_roots_root
            ON poetry_line_roots(root_buckwalter);
        CREATE INDEX IF NOT EXISTS idx_poetry_lines_poem
            ON poetry_lines(poem_id);

        -- Which (root, line) pairs the indexer has already examined, so the
        -- agent-driven loop never re-offers a line it has judged (whether or
        -- not it matched). Populated by `index-add`.
        CREATE TABLE IF NOT EXISTS poetry_root_scanned (
            root_buckwalter TEXT NOT NULL,
            line_id         INTEGER NOT NULL,
            PRIMARY KEY (root_buckwalter, line_id)
        );
        """
    )
    # Idempotent column adds — for DBs created before a column existed.
    have = {r[1] for r in conn.execute("PRAGMA table_info(poetry_poems)")}
    if "tags" not in have:
        conn.execute("ALTER TABLE poetry_poems ADD COLUMN tags TEXT")
    if "title_en" not in have:
        conn.execute("ALTER TABLE poetry_poems ADD COLUMN title_en TEXT")
    have_l = {r[1] for r in conn.execute("PRAGMA table_info(poetry_lines)")}
    if "translation_en" not in have_l:
        conn.execute("ALTER TABLE poetry_lines ADD COLUMN translation_en TEXT")
    conn.commit()


def get_claude_api_key(explicit: str | None = None) -> str | None:
    """admin_preferences.claude_api_key, else CLAUDE_API_KEY env. Mirrors
    app._get_claude_api_key without importing the heavy Flask app."""
    if explicit:
        return explicit
    try:
        conn = get_conn()
        try:
            row = conn.execute(
                "SELECT value FROM admin_preferences WHERE key = 'claude_api_key'"
            ).fetchone()
            if row and row["value"]:
                return row["value"]
        finally:
            conn.close()
    except Exception:
        pass
    return os.environ.get("CLAUDE_API_KEY") or os.environ.get("ANTHROPIC_API_KEY")


# ------------------------------------------------------------------------
# Arabic normalization
# ------------------------------------------------------------------------

def bare(s: str | None) -> str:
    """Strip ALL combining marks (category Mn) + tatweel — robust diacritic
    removal for substring/subsequence matching. Keeps base letters intact."""
    s = (s or "").replace("ـ", "")
    return "".join(ch for ch in s if unicodedata.category(ch) != "Mn")


def ordered_subseq(needle: str, hay: str) -> bool:
    """True if every char of `needle` appears in `hay` in order (gaps allowed).
    Recall-safe for SOUND roots: a derivative always contains the radicals in
    order. NOT safe for weak (و/ي) or doubled roots, which can metathesize."""
    it = iter(hay)
    return all(ch in it for ch in needle)


def normalize_ar(s: str) -> str:
    """Strip diacritics/tatweel, fold hamza/alef/ya/ta-marbuta variants, drop
    spaces — for fuzzy name matching only (never for display)."""
    if not s:
        return ""
    s = unicodedata.normalize("NFKC", s)
    s = TASHKEEL_RE.sub("", s)
    trans = {
        "أ": "ا", "إ": "ا", "آ": "ا", "ٱ": "ا",
        "ؤ": "و", "ئ": "ي", "ء": "",
        "ة": "ه", "ى": "ي",
    }
    s = "".join(trans.get(ch, ch) for ch in s)
    s = re.sub(r"\s+", "", s)
    return s


def root_arabic_for(conn: sqlite3.Connection, root_bw: str) -> str:
    """Spaced Arabic root for display, e.g. 'kfr' -> 'ك ف ر'. Prefer the
    morphology table's own spelling; fall back to the buckwalter map."""
    row = conn.execute(
        "SELECT root_arabic FROM morphology WHERE root_buckwalter = ? AND root_arabic IS NOT NULL LIMIT 1",
        (root_bw,),
    ).fetchone()
    if row and row["root_arabic"]:
        return row["root_arabic"]
    try:
        return space_root(buckwalter_to_arabic(root_bw))
    except Exception:
        return root_bw


# ------------------------------------------------------------------------
# Insertion
# ------------------------------------------------------------------------

def clean_line(s: str | None) -> str | None:
    """Remove tatweel (kashida U+0640 — decorative elongation, never semantic)
    and collapse whitespace. Applied to every stored hemistich so source
    artifacts (Wikisource justification, scrape noise) don't pollute the text
    or the root extractor."""
    if not s:
        return s
    s = s.replace("ـ", "")
    s = re.sub(r"\s+", " ", s).strip()
    return s or None


def split_hemistichs(bayt: str) -> tuple[str, str | None]:
    """Best-effort split of a full bayt into ṣadr + ʿajuz. Datasets variously
    separate the two hemistichs by a run of spaces, a tab, '...', '،', or
    common ASCII markers. If we can't split, keep the whole line as ṣadr."""
    s = bayt.strip()
    for sep in ["\t", " *** ", " ** ", " ... ", "...", " ـ ", " — ", " – "]:
        if sep in s:
            a, _, b = s.partition(sep)
            return a.strip(), (b.strip() or None)
    m = re.split(r"\s{2,}", s)
    if len(m) == 2:
        return m[0].strip(), m[1].strip()
    return s, None


def insert_poem(conn: sqlite3.Connection, p: dict, lines: list[dict]) -> int:
    cur = conn.execute(
        """INSERT INTO poetry_poems
           (poet, poet_latin, era, title, meter, rhyme, tags, auth_tier,
            source, source_ref, full_text, notes)
           VALUES (?,?,?,?,?,?,?,?,?,?,?,?)""",
        (
            p.get("poet"), p.get("poet_latin"), p.get("era"), p.get("title"),
            p.get("meter"), p.get("rhyme"), p.get("tags"),
            (p.get("auth_tier") or "C").upper(),
            p.get("source"), p.get("source_ref"), p.get("full_text"), p.get("notes"),
        ),
    )
    poem_id = cur.lastrowid
    for i, ln in enumerate(lines, start=1):
        h1 = ln.get("hemistich1")
        h2 = ln.get("hemistich2")
        if h1 is None and ln.get("text"):
            h1, h2 = split_hemistichs(ln["text"])
        h1, h2 = clean_line(h1), clean_line(h2)
        text_plain = clean_line(ln.get("text_plain")) or " ".join(x for x in [h1, h2] if x)
        conn.execute(
            """INSERT OR IGNORE INTO poetry_lines
               (poem_id, line_no, hemistich1, hemistich2, text_plain)
               VALUES (?,?,?,?,?)""",
            (poem_id, ln.get("line_no", i), h1, h2, text_plain),
        )
    return poem_id


def delete_poem(conn: sqlite3.Connection, poem_id: int) -> None:
    line_ids = [r["id"] for r in conn.execute(
        "SELECT id FROM poetry_lines WHERE poem_id = ?", (poem_id,))]
    if line_ids:
        qmarks = ",".join("?" * len(line_ids))
        conn.execute(f"DELETE FROM poetry_line_roots WHERE line_id IN ({qmarks})", line_ids)
    conn.execute("DELETE FROM poetry_lines WHERE poem_id = ?", (poem_id,))
    conn.execute("DELETE FROM poetry_poems WHERE id = ?", (poem_id,))


# ------------------------------------------------------------------------
# load-muallaqat
# ------------------------------------------------------------------------

def cmd_load_muallaqat(args) -> int:
    path = args.file or DEFAULT_MUALLAQAT_JSON
    if not os.path.exists(path):
        print(f"ERROR: seed file not found: {path}", file=sys.stderr)
        return 1
    with open(path, encoding="utf-8") as f:
        data = json.load(f)
    poems = data.get("poems") if isinstance(data, dict) else data
    if not poems:
        print("No poems in seed file.")
        return 0

    conn = get_conn()
    ensure_schema(conn)
    added = skipped = replaced = 0
    try:
        for p in poems:
            ref = p.get("source_ref") or f"{p.get('poet')}|{p.get('title')}"
            existing = conn.execute(
                "SELECT id FROM poetry_poems WHERE source_ref = ?", (ref,)
            ).fetchone()
            if existing:
                if not args.force:
                    skipped += 1
                    continue
                delete_poem(conn, existing["id"])
                replaced += 1
            p.setdefault("source", "muallaqat")
            p.setdefault("source_ref", ref)
            p.setdefault("auth_tier", "A")
            insert_poem(conn, p, p.get("lines") or [])
            added += 1
        conn.commit()
    finally:
        conn.close()
    print(f"load-muallaqat: {added} loaded ({replaced} replaced), {skipped} skipped "
          f"(already present; --force to overwrite). Source: {path}")
    return 0


# ------------------------------------------------------------------------
# load-kaggle
# ------------------------------------------------------------------------

def parse_tags(tags: str | None) -> tuple[str | None, str | None, bool]:
    """From an aldiwan-style tag string like
    'قصائد المعلقات, عموديه, بحر الطويل,  قافية اللام (ل)' pull out the
    meter (بحر …), the rhyme (قافية …), and whether it's a Muʿallaqa."""
    if not tags:
        return None, None, False
    meter = rhyme = None
    m = re.search(r"بحر\s+([^,]+)", tags)
    if m:
        meter = m.group(1).strip()
    rq = re.search(r"قافية\s+([^,]+)", tags)
    if rq:
        rhyme = rq.group(1).strip()
    is_muallaqa = "معلق" in normalize_ar(tags)
    return meter, rhyme, is_muallaqa


def detect_column(header: list[str], kind: str, override: str | None) -> str | None:
    if override:
        return override if override in header else None
    norm_header = {normalize_ar(h): h for h in header}
    lower_header = {h.lower().strip(): h for h in header}
    for cand in COL_CANDIDATES[kind]:
        if cand in header:
            return cand
        if cand.lower() in lower_header:
            return lower_header[cand.lower()]
        nc = normalize_ar(cand)
        if nc in norm_header:
            return norm_header[nc]
    return None


def cmd_load_kaggle(args) -> int:
    path = args.csv
    if not os.path.exists(path):
        print(f"ERROR: CSV not found: {path}\n"
              f"  Download mdanok/arabic-poetry-dataset from Kaggle and place it there.",
              file=sys.stderr)
        return 1
    csv.field_size_limit(10_000_000)  # some qaṣīdas are long

    if args.eras:
        want_eras = {normalize_ar(e) for e in args.eras.split(",") if e.strip()}
    else:
        want_eras = {normalize_ar(v) for v in JAHILI_ERA_VALUES}

    conn = get_conn()
    ensure_schema(conn)
    try:
        with open(path, encoding="utf-8-sig", newline="") as f:
            reader = csv.DictReader(f)
            header = reader.fieldnames or []
            cols = {k: detect_column(header, k, getattr(args, f"{k}_col", None))
                    for k in COL_CANDIDATES}
            if not cols["text"]:
                print(f"ERROR: could not find a poem-text column. Header: {header}\n"
                      f"  Pass --text-col <name>.", file=sys.stderr)
                return 1
            if not cols["era"]:
                print("WARNING: no era column detected — would load ALL rows. "
                      "Pass --era-col <name>.", file=sys.stderr)
            print(f"load-kaggle: columns -> { {k: v for k, v in cols.items() if v} }")

            source = f"kaggle:{os.path.basename(path)}"
            existing = {r["source_ref"] for r in conn.execute(
                "SELECT source_ref FROM poetry_poems WHERE source = ?", (source,))
                if r["source_ref"]}

            added = lines_total = skipped_era = skipped_dup = 0
            for row in reader:
                era_val = (row.get(cols["era"]) or "").strip() if cols["era"] else ""
                if cols["era"] and normalize_ar(era_val) not in want_eras:
                    skipped_era += 1
                    continue
                text = (row.get(cols["text"]) or "").strip()
                if not text:
                    continue
                poet = (row.get(cols["poet"]) or "").strip() if cols["poet"] else None
                title = (row.get(cols["title"]) or "").strip() if cols["title"] else None
                tags = (row.get(cols["tags"]) or "").strip() if cols["tags"] else None
                ref = f"{source}|{poet}|{title}"
                if ref in existing and not args.force:
                    skipped_dup += 1
                    continue

                meter, rhyme, _is_m = parse_tags(tags)
                shatrs = [s.strip() for s in re.split(r"[\r\n]+", text) if s.strip()]
                if not shatrs:
                    continue
                if args.granularity == "line":
                    lines = [{"text": s} for s in shatrs]
                else:  # 'pairs': aldiwan stores one hemistich per line -> (ṣadr, ʿajuz)
                    lines = [
                        {"hemistich1": shatrs[i],
                         "hemistich2": shatrs[i + 1] if i + 1 < len(shatrs) else None}
                        for i in range(0, len(shatrs), 2)
                    ]
                p = {
                    "poet": poet, "era": era_val or "jahili", "title": title,
                    "meter": meter, "rhyme": rhyme, "tags": tags, "auth_tier": "C",
                    "source": source, "source_ref": ref, "full_text": text,
                }
                insert_poem(conn, p, lines)
                existing.add(ref)
                added += 1
                lines_total += len(lines)
                if args.limit and added >= args.limit:
                    break
            conn.commit()
    finally:
        conn.close()
    print(f"load-kaggle: {added} poems ({lines_total} abyāt) loaded as Tier C "
          f"[skipped {skipped_era} off-era, {skipped_dup} already-loaded]. "
          f"Run `tiers` next to promote Muʿallaqāt / major poets.")
    return 0


# ------------------------------------------------------------------------
# tiers  (promote authentication tiers)
# ------------------------------------------------------------------------

def cmd_tiers(args) -> int:
    conn = get_conn()
    ensure_schema(conn)
    changed = {"A": 0, "B": 0}
    try:
        rows = conn.execute(
            "SELECT id, poet, title, tags, auth_tier FROM poetry_poems").fetchall()
        for r in rows:
            poet_n = normalize_ar(r["poet"] or "")
            marker_n = normalize_ar((r["title"] or "") + " " + (r["tags"] or ""))
            cur_tier = (r["auth_tier"] or "C").upper()
            target = None
            is_muallaqa_poet = any(k in poet_n for k in MUALLAQAT_POET_KEYS)
            is_major = any(k in poet_n for k in MAJOR_JAHILI_POET_KEYS)
            # The Muʿallaqa marker lives in the tags ('قصائد المعلقات') for the
            # scraped corpus, and in the title for the hand-curated seed.
            if is_muallaqa_poet and "معلق" in marker_n:
                target = "A"
            elif is_muallaqa_poet:
                target = "B"
            elif is_major:
                target = "B"
            if target and TIER_RANK[target] > TIER_RANK[cur_tier]:
                conn.execute("UPDATE poetry_poems SET auth_tier = ? WHERE id = ?",
                             (target, r["id"]))
                changed[target] += 1
        conn.commit()
    finally:
        conn.close()
    print(f"tiers: promoted {changed['A']} poem(s) to A, {changed['B']} to B "
          f"(never downgrades; hand-curated A stays A).")
    return 0


# ------------------------------------------------------------------------
# index-root  (LLM root extraction)
# ------------------------------------------------------------------------

EXTRACT_SYSTEM = """\
You are a classical-Arabic morphology specialist. You will be given ONE
triliteral (or quadriliteral) Arabic root and a numbered list of lines of
pre-Islamic poetry. Your job: find every line that contains a word
DERIVED from that exact root — and ignore coincidental letter overlap.

Rules:
  - Judge by the genuine morphological root, not surface letters. A word
    shares the root only if its consonantal skeleton genuinely derives from
    it under normal Arabic derivation (counting weak-letter elision,
    assimilation, gemination, hamza shifts).
  - Do NOT include a line just because the three letters appear; e.g. a
    different word that merely contains ك, ف, ر in sequence is not K-F-R
    unless it really derives from كفر.
  - For each genuine match, give the surface word as it appears and a
    one-line sense of how it is used IN THAT LINE (physical? metaphor?).
  - Return STRICT JSON only. If no line matches, return an empty array.

Output ONLY a JSON array, no preamble:
[
  {"line_id": <int from the list>, "surface_word": "<word as written>",
   "sense_hint": "<≤12 words: how it's used here>", "confidence": 0.0-1.0},
  ...
]
"""


def call_claude(model: str, system: str, user: str, api_key: str) -> str:
    last_err = None
    for attempt in range(1, MAX_ATTEMPTS + 1):
        try:
            resp = requests.post(
                ANTHROPIC_URL,
                headers={
                    "Content-Type": "application/json",
                    "x-api-key": api_key,
                    "anthropic-version": "2023-06-01",
                },
                json={
                    "model": model,
                    "max_tokens": MAX_TOKENS,
                    "system": system,
                    "messages": [{"role": "user", "content": user}],
                },
                timeout=300,
            )
        except requests.RequestException as e:
            last_err = f"request error: {e}"
        else:
            if resp.status_code == 200:
                blocks = resp.json().get("content") or []
                return "".join(b.get("text", "") for b in blocks if b.get("type") == "text")
            last_err = f"HTTP {resp.status_code}: {resp.text[:300]}"
        if attempt < MAX_ATTEMPTS:
            time.sleep((2 ** attempt) + 1)
    raise RuntimeError(f"Claude failed after {MAX_ATTEMPTS} attempts: {last_err}")


def parse_json_array(raw: str) -> list:
    """Robustly extract a JSON array even if the model wraps it in fences or
    adds a stray sentence. Bracket-matches from the first '[' to its partner so
    trailing prose can't cause an 'Extra data' failure (which would silently
    drop a chunk's matches)."""
    text = (raw or "").strip()
    text = re.sub(r"^```(?:json)?\s*\n?", "", text)
    text = re.sub(r"\n?```\s*$", "", text).strip()
    try:
        v = json.loads(text)
        return v if isinstance(v, list) else []
    except Exception:
        pass
    start = text.find("[")
    if start < 0:
        return []
    depth = 0
    for i in range(start, len(text)):
        if text[i] == "[":
            depth += 1
        elif text[i] == "]":
            depth -= 1
            if depth == 0:
                try:
                    v = json.loads(text[start:i + 1])
                    return v if isinstance(v, list) else []
                except Exception:
                    return []
    return []


def cmd_index_root(args) -> int:
    root_bw = args.root
    tiers = [t.upper() for t in args.tiers]
    conn = get_conn()
    ensure_schema(conn)
    try:
        root_ar = root_arabic_for(conn, root_bw)
        qmarks = ",".join("?" * len(tiers))
        lines = conn.execute(
            f"""SELECT pl.id, pl.text_plain, pp.poet, pp.auth_tier
                FROM poetry_lines pl JOIN poetry_poems pp ON pp.id = pl.poem_id
                WHERE pp.auth_tier IN ({qmarks})
                ORDER BY pp.auth_tier DESC, pl.poem_id, pl.line_no""",
            tiers,
        ).fetchall()
        if not lines:
            print(f"index-root {root_bw}: no poetry lines in tiers {tiers}. "
                  f"Load + tier the corpus first.")
            return 0

        if not args.force:
            done = {r["line_id"] for r in conn.execute(
                "SELECT line_id FROM poetry_line_roots WHERE root_buckwalter = ?",
                (root_bw,))}
            lines = [l for l in lines if l["id"] not in done]

        prefiltered = len(lines)
        if args.prefilter:
            radicals = bare(root_ar).replace(" ", "")
            lines = [l for l in lines if ordered_subseq(radicals, bare(l["text_plain"]))]
            print(f"  prefilter (subsequence {radicals}): {prefiltered} -> {len(lines)} "
                  f"candidate lines. (Recall-safe for SOUND roots only.)")
        if args.limit:
            lines = lines[: args.limit]

        print(f"index-root {root_bw} ({root_ar}): scanning {len(lines)} lines "
              f"in tiers {tiers} with {args.model} ...")

        api_key = get_claude_api_key(args.api_key)
        if not api_key and not args.dry_run:
            print("ERROR: no Claude API key (admin_preferences/CLAUDE_API_KEY/--api-key).",
                  file=sys.stderr)
            return 1

        total_found = 0
        for start in range(0, len(lines), LINES_PER_CHUNK):
            chunk = lines[start:start + LINES_PER_CHUNK]
            listing = "\n".join(f"  [{l['id']}] {l['text_plain']}" for l in chunk)
            user = (f"ROOT: {root_ar}  (Buckwalter: {root_bw})\n\n"
                    f"LINES (each prefixed with its line_id in brackets):\n{listing}\n\n"
                    f"Return the JSON array of lines that genuinely use this root.")
            if args.dry_run:
                print(f"\n--- chunk {start//LINES_PER_CHUNK + 1} prompt ({len(chunk)} lines) ---")
                print(user[:1200])
                print("  ...(dry-run: not calling Claude)")
                continue

            raw = call_claude(args.model, EXTRACT_SYSTEM, user, api_key)
            try:
                matches = parse_json_array(raw)
            except Exception as e:
                print(f"  chunk {start//LINES_PER_CHUNK + 1}: parse error {e}", file=sys.stderr)
                continue

            valid_ids = {l["id"] for l in chunk}
            for m in matches:
                if isinstance(m, int):            # model returned bare line_ids
                    m = {"line_id": m}
                if not isinstance(m, dict):
                    continue
                lid = m.get("line_id")
                if lid not in valid_ids:
                    continue  # reject hallucinated/foreign line ids
                conn.execute(
                    """INSERT OR REPLACE INTO poetry_line_roots
                       (line_id, root_buckwalter, root_arabic, surface_word,
                        sense_hint, extractor_model, confidence, verified)
                       VALUES (?,?,?,?,?,?,?,
                               COALESCE((SELECT verified FROM poetry_line_roots
                                         WHERE line_id=? AND root_buckwalter=?), 0))""",
                    (lid, root_bw, root_ar, m.get("surface_word"),
                     m.get("sense_hint"), args.model, m.get("confidence"),
                     lid, root_bw),
                )
                total_found += 1
            conn.commit()
            print(f"  chunk {start//LINES_PER_CHUNK + 1}/"
                  f"{(len(lines)+LINES_PER_CHUNK-1)//LINES_PER_CHUNK}: "
                  f"{len(matches)} match(es)")

        if not args.dry_run:
            print(f"index-root {root_bw}: {total_found} candidate occurrence(s) indexed "
                  f"(verified=0). Run `verify-root {root_bw}` to confirm quotable lines.")
    finally:
        conn.close()
    return 0


# ------------------------------------------------------------------------
# index-next / index-add  (AGENT-DRIVEN indexing — no API; Claude Code judges)
# ------------------------------------------------------------------------

def cmd_index_next(args) -> int:
    """Emit the next batch of un-scanned candidate lines for a root, as JSON,
    for the Claude Code indexing loop to judge. Prefilters to likely candidates
    (surface forms by default) so batches aren't mostly noise."""
    conn = get_conn()
    ensure_schema(conn)
    try:
        tiers = [t.upper() for t in args.tiers]
        root_ar = root_arabic_for(conn, args.root)
        qmarks = ",".join("?" * len(tiers))
        rows = conn.execute(
            f"""SELECT pl.id, pl.text_plain
                FROM poetry_lines pl JOIN poetry_poems pp ON pp.id = pl.poem_id
                WHERE pp.auth_tier IN ({qmarks})
                  AND pl.id NOT IN (SELECT line_id FROM poetry_root_scanned
                                    WHERE root_buckwalter = ?)
                ORDER BY pp.auth_tier DESC, pl.poem_id, pl.line_no""",
            (*tiers, args.root),
        ).fetchall()

        forms = None
        if args.forms:
            forms = [bare(f) for f in args.forms.split(",") if f.strip()]
        elif not args.no_prefilter:
            forms = [bare(f) for f in (SURFACE_FORMS.get(args.root) or [])]
        if forms:
            rows = [r for r in rows if any(f in bare(r["text_plain"]) for f in forms)]
        elif args.subseq:
            rad = bare(root_ar).replace(" ", "")
            rows = [r for r in rows if ordered_subseq(rad, bare(r["text_plain"]))]

        batch = rows[: args.count]
        out = {
            "root": args.root, "root_arabic": root_ar, "tiers": tiers,
            "remaining": len(rows), "batch": len(batch),
            "lines": [{"id": r["id"], "text": r["text_plain"]} for r in batch],
        }
        print(json.dumps(out, ensure_ascii=False, indent=2))
    finally:
        conn.close()
    return 0


def cmd_index_add(args) -> int:
    """Store the agent's verdicts. Payload JSON:
       {"scanned": [<all line_ids in the judged batch>],
        "matches": [{"line_id", "surface_word", "sense_hint", "confidence"}, ...]}
    Every scanned line is marked done (so it's never re-offered); only genuine
    matches go into poetry_line_roots. A match must be among the scanned ids."""
    with open(args.file, encoding="utf-8") as f:
        payload = json.load(f)
    scanned = [int(x) for x in (payload.get("scanned") or [])]
    matches = payload.get("matches") or []
    conn = get_conn()
    ensure_schema(conn)
    try:
        root_ar = root_arabic_for(conn, args.root)
        scanned_set = set(scanned)
        for lid in scanned:
            conn.execute(
                "INSERT OR IGNORE INTO poetry_root_scanned (root_buckwalter, line_id) VALUES (?,?)",
                (args.root, lid))
        stored = rejected = 0
        for m in matches:
            lid = m.get("line_id")
            if lid not in scanned_set:
                rejected += 1
                continue  # a match must be a line we were actually given
            conn.execute(
                """INSERT OR REPLACE INTO poetry_line_roots
                   (line_id, root_buckwalter, root_arabic, surface_word, sense_hint,
                    extractor_model, confidence, verified)
                   VALUES (?,?,?,?,?,?,?,
                           COALESCE((SELECT verified FROM poetry_line_roots
                                     WHERE line_id=? AND root_buckwalter=?), 0))""",
                (lid, args.root, root_ar, m.get("surface_word"), m.get("sense_hint"),
                 "claude-code", m.get("confidence"), lid, args.root))
            stored += 1
        conn.commit()
        rej = f", {rejected} rejected (not in batch)" if rejected else ""
        print(f"index-add {args.root}: {len(scanned)} scanned, {stored} match(es) stored{rej}.")
    finally:
        conn.close()
    return 0


# ------------------------------------------------------------------------
# verify-root
# ------------------------------------------------------------------------

def cmd_verify_root(args) -> int:
    conn = get_conn()
    ensure_schema(conn)
    try:
        if args.set:
            ids = [int(x) for x in args.set]
            qmarks = ",".join("?" * len(ids))
            conn.execute(
                f"UPDATE poetry_line_roots SET verified = 1 WHERE id IN ({qmarks})", ids)
            conn.commit()
            print(f"verify-root: marked {len(ids)} line_root id(s) verified.")
            return 0
        if args.all:
            n = conn.execute(
                "UPDATE poetry_line_roots SET verified = 1 WHERE root_buckwalter = ?",
                (args.root,)).rowcount
            conn.commit()
            print(f"verify-root {args.root}: marked all {n} candidate(s) verified.")
            return 0

        rows = conn.execute(
            """SELECT plr.id, plr.surface_word, plr.sense_hint, plr.confidence,
                      plr.verified, pp.poet, pp.auth_tier, pl.text_plain
               FROM poetry_line_roots plr
               JOIN poetry_lines pl ON pl.id = plr.line_id
               JOIN poetry_poems pp ON pp.id = pl.poem_id
               WHERE plr.root_buckwalter = ?
               ORDER BY pp.auth_tier DESC, plr.confidence DESC""",
            (args.root,)).fetchall()
        if not rows:
            print(f"verify-root {args.root}: no candidates indexed yet.")
            return 0
        print(f"verify-root {args.root}: {len(rows)} candidate(s). "
              f"Mark good ones with: verify-root {args.root} --set <id> [--set <id> ...]\n")
        for r in rows:
            mark = "✓" if r["verified"] else " "
            print(f"  [{mark}] id={r['id']:<5} tier={r['auth_tier']} "
                  f"conf={r['confidence']}  «{r['surface_word']}» — {r['sense_hint']}")
            print(f"        {r['poet'] or '?'}: {r['text_plain']}")
    finally:
        conn.close()
    return 0


# ------------------------------------------------------------------------
# stats
# ------------------------------------------------------------------------

PILOT_ROOTS = ["kfr", "wqy", "dhr", "krm", "jnn"]


def cmd_stats(args) -> int:
    conn = get_conn()
    ensure_schema(conn)
    try:
        print("━━━━━ poetry corpus ━━━━━")
        tier_rows = conn.execute(
            "SELECT auth_tier, COUNT(*) n FROM poetry_poems GROUP BY auth_tier ORDER BY auth_tier"
        ).fetchall()
        npoems = conn.execute("SELECT COUNT(*) n FROM poetry_poems").fetchone()["n"]
        nlines = conn.execute("SELECT COUNT(*) n FROM poetry_lines").fetchone()["n"]
        print(f"poems: {npoems}   lines: {nlines}")
        for r in tier_rows:
            print(f"  tier {r['auth_tier']}: {r['n']} poem(s)")

        print("\n━━━━━ root index ━━━━━")
        idx_rows = conn.execute(
            """SELECT root_buckwalter,
                      COUNT(*) n,
                      SUM(verified) v
               FROM poetry_line_roots GROUP BY root_buckwalter
               ORDER BY n DESC""").fetchall()
        if not idx_rows:
            print("  (no roots indexed yet — run index-root <bw>)")
        for r in idx_rows:
            print(f"  {r['root_buckwalter']:<6} {r['n']:>4} candidate(s), "
                  f"{r['v'] or 0} verified")

        print("\n━━━━━ pilot roots ━━━━━")
        for rb in PILOT_ROOTS:
            row = conn.execute(
                "SELECT COUNT(*) n, SUM(verified) v FROM poetry_line_roots WHERE root_buckwalter = ?",
                (rb,)).fetchone()
            print(f"  {rb:<6} {root_arabic_for(conn, rb):<10} "
                  f"poetry: {row['n']} cand / {row['v'] or 0} verified")
    finally:
        conn.close()
    return 0


# ------------------------------------------------------------------------
# main
# ------------------------------------------------------------------------

# ------------------------------------------------------------------------
# Full-poem translation (Phase 3) — only the poems quoted by an approved
# comparison/note get translated. Agent-driven loop, no API.
# ------------------------------------------------------------------------

def quoted_poem_ids(conn) -> set:
    """Poem ids referenced by any quoted line in the approved comparisons,
    verse notes, or the contemporaneous lexicon."""
    lrids = set()
    for tbl in ("root_poetry_comparisons", "verse_poetry_notes", "root_poetic_lexicon"):
        try:
            for r in conn.execute(f"SELECT quoted_lines_json FROM {tbl} "
                                  "WHERE quoted_lines_json IS NOT NULL"):
                try:
                    for q in json.loads(r["quoted_lines_json"]):
                        if q.get("line_root_id"):
                            lrids.add(q["line_root_id"])
                except Exception:
                    pass
        except sqlite3.OperationalError:
            pass  # table not present yet
    if not lrids:
        return set()
    qmarks = ",".join("?" * len(lrids))
    rows = conn.execute(
        f"""SELECT DISTINCT pl.poem_id FROM poetry_line_roots plr
            JOIN poetry_lines pl ON pl.id = plr.line_id
            WHERE plr.id IN ({qmarks})""", list(lrids)).fetchall()
    return {r["poem_id"] for r in rows}


def cmd_trans_next(args) -> int:
    conn = get_conn(); ensure_schema(conn)
    try:
        todo = []
        for pid in sorted(quoted_poem_ids(conn)):
            tot = conn.execute("SELECT COUNT(*) n FROM poetry_lines WHERE poem_id=?",
                               (pid,)).fetchone()["n"]
            done = conn.execute("SELECT COUNT(*) n FROM poetry_lines WHERE poem_id=? "
                                "AND translation_en IS NOT NULL AND translation_en!=''",
                                (pid,)).fetchone()["n"]
            if done < tot:
                poet = conn.execute("SELECT poet FROM poetry_poems WHERE id=?",
                                    (pid,)).fetchone()["poet"]
                todo.append({"poem_id": pid, "poet": poet, "lines": tot, "translated": done})
        print(json.dumps({
            "poems_remaining": len(todo),
            "lines_remaining": sum(t["lines"] - t["translated"] for t in todo),
            "poems": todo[: args.count],
        }, ensure_ascii=False, indent=2))
    finally:
        conn.close()
    return 0


def cmd_trans_context(args) -> int:
    conn = get_conn(); ensure_schema(conn)
    try:
        pid = int(args.poem)
        p = conn.execute("SELECT poet, title, meter, rhyme, era FROM poetry_poems WHERE id=?",
                         (pid,)).fetchone()
        lines = conn.execute(
            "SELECT id, line_no, hemistich1, hemistich2, text_plain, translation_en "
            "FROM poetry_lines WHERE poem_id=? ORDER BY line_no", (pid,)).fetchall()
        untrans = [ln for ln in lines if not ln["translation_en"]]
        L = [f"# Poem {pid} — {p['poet']} · {p['title'] or ''}  (meter {p['meter'] or '?'}, "
             f"rhyme {p['rhyme'] or '?'})",
             f"Untranslated lines: {len(untrans)} of {len(lines)} (showing up to {args.limit}).",
             "Translate each line into clear, faithful, readable English — one English line per "
             "bayt. Keep the line_id.", ""]
        for ln in untrans[: args.limit]:
            arabic = ln["text_plain"] or " / ".join(x for x in [ln["hemistich1"], ln["hemistich2"]] if x)
            L.append(f"[{ln['id']}] (bayt {ln['line_no']}) {arabic}")
        print("\n".join(L))
    finally:
        conn.close()
    return 0


def cmd_trans_add(args) -> int:
    with open(args.file, encoding="utf-8") as f:
        payload = json.load(f)
    items = payload.get("translations") if isinstance(payload, dict) else payload
    conn = get_conn(); ensure_schema(conn)
    try:
        n = 0
        for it in (items or []):
            lid, en = it.get("line_id"), (it.get("english") or "").strip()
            if lid and en:
                conn.execute("UPDATE poetry_lines SET translation_en=? WHERE id=?", (en, lid))
                n += 1
        conn.commit()
        print(f"trans-add: stored {n} line translation(s).")
    finally:
        conn.close()
    return 0


# ---- Meter (baḥr) teaching pages -------------------------------------------
# Mirror of app.py's METER_REGISTRY: one page per base meter, variants folded in.
# Kept in sync by hand (small, static). Order = teaching priority.
METER_REGISTRY = [
    {"key": "tawil", "ar": "الطويل", "en": "Ṭawīl", "meaning": "the long",
     "variants": ["مجزوء الطويل"]},
    {"key": "wafir", "ar": "الوافر", "en": "Wāfir", "meaning": "the abundant",
     "variants": ["مجزوء الوافر"]},
    {"key": "basit", "ar": "البسيط", "en": "Basīṭ", "meaning": "the outspread",
     "variants": ["مجزوء البسيط", "مخلع البسيط"]},
    {"key": "kamil", "ar": "الكامل", "en": "Kāmil", "meaning": "the complete",
     "variants": ["مجزوء الكامل", "أحذ الكامل"]},
    {"key": "mutaqarib", "ar": "المتقارب", "en": "Mutaqārib", "meaning": "the nearing",
     "variants": []},
    {"key": "khafif", "ar": "الخفيف", "en": "Khafīf", "meaning": "the light",
     "variants": ["مجزوء الخفيف"]},
    {"key": "rajaz", "ar": "الرجز", "en": "Rajaz", "meaning": "the quivering",
     "variants": ["مجزوء الرجز", "مشطور الرجز"]},
    {"key": "ramal", "ar": "الرمل", "en": "Ramal", "meaning": "the trotting",
     "variants": ["مجزوء الرمل"]},
    {"key": "sari", "ar": "السريع", "en": "Sarīʿ", "meaning": "the swift", "variants": []},
    {"key": "munsarih", "ar": "المنسرح", "en": "Munsariḥ", "meaning": "the flowing",
     "variants": []},
    {"key": "madid", "ar": "المديد", "en": "Madīd", "meaning": "the outstretched",
     "variants": []},
    {"key": "hazaj", "ar": "الهزج", "en": "Hazaj", "meaning": "the lilting", "variants": []},
    {"key": "mujtathth", "ar": "المجتث", "en": "Mujtathth", "meaning": "the uprooted",
     "variants": []},
]
_METER_BY_KEY = {m["key"]: m for m in METER_REGISTRY}


def _meter_values(entry):
    return [entry["ar"], *entry["variants"]]


def ensure_meter_table(conn: sqlite3.Connection) -> None:
    """Idempotent: same shape app.py serves from."""
    conn.executescript(
        """
        CREATE TABLE IF NOT EXISTS meter_articles (
            id               INTEGER PRIMARY KEY AUTOINCREMENT,
            meter_key        TEXT UNIQUE NOT NULL,
            meter_ar         TEXT,
            name_en          TEXT,
            name_meaning     TEXT,
            tafil_ar         TEXT,
            tafil_latin      TEXT,
            syllable_pattern TEXT,
            mnemonic_en      TEXT,
            article_markdown TEXT,
            showcase_json    TEXT,
            variants_json    TEXT,
            confidence       REAL,
            qa_status        TEXT,
            qa_notes         TEXT,
            review_status    TEXT DEFAULT 'pending',
            hidden           INTEGER DEFAULT 0,
            raw_response     TEXT,
            created_at       TEXT DEFAULT CURRENT_TIMESTAMP,
            edited_at        TEXT
        );
        """
    )
    conn.commit()


def cmd_meter_next(args) -> int:
    """List base meters that still need a teaching article (no row yet, or a
    row flagged needs_fix by QA)."""
    conn = get_conn(); ensure_schema(conn); ensure_meter_table(conn)
    try:
        counts = {}
        for r in conn.execute("SELECT meter, COUNT(*) c FROM poetry_poems "
                              "WHERE meter IS NOT NULL AND meter<>'' GROUP BY meter"):
            counts[r["meter"]] = r["c"]
        todo = []
        for m in METER_REGISTRY:
            row = conn.execute(
                "SELECT qa_status, article_markdown FROM meter_articles WHERE meter_key=?",
                (m["key"],)).fetchone()
            needs = (row is None) or (row["qa_status"] == "needs_fix") \
                or (not (row["article_markdown"] or "").strip())
            if needs:
                todo.append({"key": m["key"], "meter_ar": m["ar"], "name_en": m["en"],
                             "name_meaning": m["meaning"],
                             "poem_count": sum(counts.get(v, 0) for v in _meter_values(m))})
        todo.sort(key=lambda x: x["poem_count"], reverse=True)
        print(json.dumps({"meters_remaining": len(todo),
                          "meters": todo[: args.count]}, ensure_ascii=False, indent=2))
    finally:
        conn.close()
    return 0


def cmd_meter_context(args) -> int:
    """Print one meter's identity + variant strings + a pool of candidate
    showcase lines (authentic, already-translated) for the agent to choose from."""
    entry = _METER_BY_KEY.get((args.key or "").strip())
    if not entry:
        print(f"Unknown meter key: {args.key}", file=sys.stderr); return 2
    conn = get_conn(); ensure_schema(conn)
    try:
        values = _meter_values(entry)
        counts = {}
        for r in conn.execute("SELECT meter, COUNT(*) c FROM poetry_poems "
                              "WHERE meter IN (%s) GROUP BY meter" % ",".join("?" * len(values)),
                              values):
            counts[r["meter"]] = r["c"]
        L = [f"# Meter '{entry['key']}' — {entry['ar']} (\"{entry['en']}\", {entry['meaning']})",
             f"Base value: {entry['ar']}  ({counts.get(entry['ar'], 0)} poems)"]
        for v in entry["variants"]:
            if counts.get(v, 0):
                L.append(f"Variant: {v}  ({counts[v]} poems) — a clipped/halved form, folds into this page.")
        L += ["",
              "TASK: write a beginner-friendly English teaching page for this meter, then pick "
              "2–4 of the candidate lines below as showcase verses. Provide for each chosen line a "
              "read-aloud transliteration and a long/short scansion that matches the meter's tafāʿīl.",
              "",
              "Candidate showcase lines (authentic poems in this meter, already translated — "
              "choose lines that scan cleanly):", ""]
        # prefer authentic tiers + present translations; cap the pool
        cand = conn.execute(
            "SELECT pl.id, pl.line_no, pl.hemistich1, pl.hemistich2, pl.text_plain, "
            "       pl.translation_en, pp.id AS poem_id, pp.poet, pp.poet_latin, pp.auth_tier "
            "FROM poetry_lines pl JOIN poetry_poems pp ON pp.id = pl.poem_id "
            "WHERE pp.meter IN (%s) AND pl.translation_en IS NOT NULL AND pl.translation_en<>'' "
            "ORDER BY CASE pp.auth_tier WHEN 'A' THEN 0 WHEN 'B' THEN 1 ELSE 2 END, pl.id "
            "LIMIT ?" % ",".join("?" * len(values)),
            [*values, args.limit]).fetchall()
        for c in cand:
            arabic = c["text_plain"] or " / ".join(
                x for x in [c["hemistich1"], c["hemistich2"]] if x)
            poet = c["poet_latin"] or c["poet"] or "?"
            L.append(f"[{c['id']}] (poem {c['poem_id']} · {poet} · tier {c['auth_tier']}) {arabic}")
            L.append(f"     ↳ {c['translation_en']}")
        print("\n".join(L))
    finally:
        conn.close()
    return 0


def cmd_meter_add(args) -> int:
    """Upsert a meter article (review_status='pending'). Showcase Arabic/poet are
    re-derived from the DB by line_id so the example text is always real; the
    agent supplies transliteration, scansion and (optionally) a refined gloss."""
    entry = _METER_BY_KEY.get((args.key or "").strip())
    if not entry:
        print(f"Unknown meter key: {args.key}", file=sys.stderr); return 2
    with open(args.file, encoding="utf-8") as f:
        payload = json.load(f)
    conn = get_conn(); ensure_schema(conn); ensure_meter_table(conn)
    try:
        showcase = []
        for it in (payload.get("showcase") or []):
            lid = it.get("line_id")
            if not lid:
                continue
            ln = conn.execute(
                "SELECT pl.line_no, pl.hemistich1, pl.hemistich2, pl.text_plain, "
                "pl.translation_en, pp.id AS poem_id, pp.poet, pp.poet_latin "
                "FROM poetry_lines pl JOIN poetry_poems pp ON pp.id = pl.poem_id "
                "WHERE pl.id = ?", (lid,)).fetchone()
            if not ln:
                continue
            arabic = ln["text_plain"] or " / ".join(
                x for x in [ln["hemistich1"], ln["hemistich2"]] if x)
            showcase.append({
                "line_id": lid, "poem_id": ln["poem_id"], "line_no": ln["line_no"],
                "poet": ln["poet"], "poet_latin": ln["poet_latin"], "arabic": arabic,
                "transliteration": (it.get("transliteration") or "").strip(),
                "scansion": (it.get("scansion") or "").strip(),
                "translation": (it.get("translation") or ln["translation_en"] or "").strip(),
            })
        cols = {
            "meter_key": entry["key"],
            "meter_ar": payload.get("meter_ar") or entry["ar"],
            "name_en": payload.get("name_en") or entry["en"],
            "name_meaning": payload.get("name_meaning") or entry["meaning"],
            "tafil_ar": (payload.get("tafil_ar") or "").strip(),
            "tafil_latin": (payload.get("tafil_latin") or "").strip(),
            "syllable_pattern": (payload.get("syllable_pattern") or "").strip(),
            "mnemonic_en": (payload.get("mnemonic_en") or "").strip(),
            "article_markdown": (payload.get("article_markdown") or "").strip(),
            "showcase_json": json.dumps(showcase, ensure_ascii=False),
            "variants_json": json.dumps(
                [{"meter_ar": v} for v in entry["variants"]], ensure_ascii=False),
            "confidence": payload.get("confidence"),
            "qa_status": payload.get("qa_status"),
            "qa_notes": payload.get("qa_notes"),
            "raw_response": payload.get("raw_response"),
        }
        keys = list(cols.keys())
        conn.execute(
            "INSERT INTO meter_articles (%s, edited_at) VALUES (%s, datetime('now')) "
            "ON CONFLICT(meter_key) DO UPDATE SET %s, edited_at=datetime('now')" % (
                ", ".join(keys), ", ".join("?" * len(keys)),
                ", ".join(f"{k}=excluded.{k}" for k in keys if k != "meter_key")),
            [cols[k] for k in keys])
        conn.commit()
        print(f"meter-add: stored '{entry['key']}' "
              f"({len(showcase)} showcase line(s), qa={cols['qa_status'] or '—'}).")
    finally:
        conn.close()
    return 0


# ---- English poem titles + poet romanisation -------------------------------
def cmd_poets_missing(args) -> int:
    """Distinct poet names (Arabic) that have no Latin romanisation yet."""
    conn = get_conn(); ensure_schema(conn)
    try:
        rows = conn.execute(
            "SELECT poet, COUNT(*) c FROM poetry_poems "
            "WHERE poet IS NOT NULL AND poet<>'' AND (poet_latin IS NULL OR poet_latin='') "
            "GROUP BY poet ORDER BY c DESC").fetchall()
        print(json.dumps({"poets": [{"poet": r["poet"], "poems": r["c"]} for r in rows]},
                         ensure_ascii=False, indent=2))
    finally:
        conn.close()
    return 0


def cmd_poets_add(args) -> int:
    """Apply {poets:[{poet, poet_latin}]} to every row of each poet still missing it."""
    payload = json.load(open(args.file, encoding="utf-8"))
    conn = get_conn(); ensure_schema(conn)
    try:
        n = 0
        for it in (payload.get("poets") or []):
            poet, latin = it.get("poet"), (it.get("poet_latin") or "").strip()
            if poet and latin:
                cur = conn.execute(
                    "UPDATE poetry_poems SET poet_latin=? WHERE poet=? "
                    "AND (poet_latin IS NULL OR poet_latin='')", (latin, poet))
                n += cur.rowcount
        conn.commit()
        print(f"poets-add: romanised {n} poem row(s).")
    finally:
        conn.close()
    return 0


def cmd_titles_context(args) -> int:
    """For each poem id: poet, Arabic title, and first line (Arabic + English if
    translated) — enough for an agent to write a concise English title."""
    ids = [int(x) for x in str(args.ids).split(",") if x.strip().isdigit()]
    conn = get_conn(); ensure_schema(conn)
    try:
        out = []
        for pid in ids:
            p = conn.execute("SELECT poet, poet_latin, title FROM poetry_poems WHERE id=?",
                             (pid,)).fetchone()
            if not p:
                continue
            l1 = conn.execute(
                "SELECT hemistich1, hemistich2, text_plain, translation_en "
                "FROM poetry_lines WHERE poem_id=? ORDER BY line_no LIMIT 1", (pid,)).fetchone()
            l1_ar = None
            if l1:
                l1_ar = l1["text_plain"] or " ".join(
                    x for x in [l1["hemistich1"], l1["hemistich2"]] if x)
            out.append({
                "poem_id": pid, "poet": p["poet"], "poet_latin": p["poet_latin"],
                "title_ar": p["title"], "line1_ar": l1_ar,
                "line1_en": l1["translation_en"] if l1 else None,
            })
        print(json.dumps({"poems": out}, ensure_ascii=False, indent=2))
    finally:
        conn.close()
    return 0


def cmd_titles_add(args) -> int:
    """Apply {titles:[{poem_id, title_en}]} to poetry_poems."""
    payload = json.load(open(args.file, encoding="utf-8"))
    items = payload.get("titles") if isinstance(payload, dict) else payload
    conn = get_conn(); ensure_schema(conn)
    try:
        n = 0
        for it in (items or []):
            pid, t = it.get("poem_id"), (it.get("title_en") or "").strip()
            if pid and t:
                conn.execute("UPDATE poetry_poems SET title_en=? WHERE id=?", (t[:200], pid))
                n += 1
        conn.commit()
        print(f"titles-add: stored {n} English title(s).")
    finally:
        conn.close()
    return 0


def main() -> int:
    p = argparse.ArgumentParser(description=__doc__.split("\n\n", 1)[0])
    sub = p.add_subparsers(dest="cmd", required=True)

    sub.add_parser("init", help="create the corpus schema only")

    sp = sub.add_parser("load-muallaqat", help="load the hand-curated Tier-A seed JSON")
    sp.add_argument("--file", default=None, help=f"default: {DEFAULT_MUALLAQAT_JSON}")
    sp.add_argument("--force", action="store_true", help="overwrite existing poems")

    sp = sub.add_parser("load-kaggle", help="load a scraped CSV, filtered to Jahilī (Tier C)")
    sp.add_argument("csv", help="path to the Kaggle CSV")
    sp.add_argument("--eras", default=None,
                    help="comma-separated era values to keep (default: Jahilī only). "
                         "e.g. --eras 'العصر الجاهلي,المخضرمون'")
    sp.add_argument("--granularity", choices=["pairs", "line"], default="pairs",
                    help="'pairs' (default): one hemistich per source line -> pair into "
                         "abyāt. 'line': each source line is a full bayt.")
    sp.add_argument("--limit", type=int, default=0, help="cap poems (for testing)")
    sp.add_argument("--force", action="store_true", help="reload poems already present")
    for k in COL_CANDIDATES:
        sp.add_argument(f"--{k}-col", default=None, help=f"override the {k} column name")

    sub.add_parser("tiers", help="promote Muʿallaqāt / major-poet poems to A/B")

    sp = sub.add_parser("index-root", help="LLM-extract a root's occurrences in the poetry")
    sp.add_argument("root", help="Buckwalter root, e.g. kfr")
    sp.add_argument("--tiers", default="AB", help="which tiers to scan (default AB)")
    sp.add_argument("--model", default=EXTRACT_MODEL,
                    help=f"default {EXTRACT_MODEL} (extraction is mechanical & cheap)")
    sp.add_argument("--prefilter", action="store_true",
                    help="only send lines whose bare text contains the radicals as an "
                         "ordered subsequence — big cost saver, RECALL-SAFE FOR SOUND "
                         "ROOTS ONLY (not weak و/ي or doubled roots like wqy, jnn)")
    sp.add_argument("--limit", type=int, default=0, help="cap lines (for testing)")
    sp.add_argument("--force", action="store_true", help="re-index already-scanned lines")
    sp.add_argument("--dry-run", action="store_true", help="print prompts, don't call Claude")
    sp.add_argument("--api-key", default=None)

    sp = sub.add_parser("index-next",
                        help="emit next batch of candidate lines (JSON) for the Claude Code loop")
    sp.add_argument("root", help="Buckwalter root")
    sp.add_argument("--tiers", default="AB", help="tiers to scan (default AB)")
    sp.add_argument("--count", type=int, default=40, help="lines per batch (default 40)")
    sp.add_argument("--forms", default=None,
                    help="comma-separated bare surface forms to prefilter by (overrides defaults)")
    sp.add_argument("--subseq", action="store_true",
                    help="prefilter by ordered-subsequence of radicals (sound roots) "
                         "if no surface forms are used")
    sp.add_argument("--no-prefilter", action="store_true", help="scan all lines, no prefilter")

    sp = sub.add_parser("index-add", help="store the loop's verdicts (scanned + matches)")
    sp.add_argument("root", help="Buckwalter root")
    sp.add_argument("--file", required=True, help="JSON: {scanned:[...], matches:[...]}")

    sp = sub.add_parser("verify-root", help="list / confirm a root's candidate lines")
    sp.add_argument("root", help="Buckwalter root")
    sp.add_argument("--set", action="append", default=[], help="line_root id(s) to mark verified")
    sp.add_argument("--all", action="store_true", help="mark ALL candidates verified (use with care)")

    sub.add_parser("stats", help="corpus + index coverage")

    sp = sub.add_parser("trans-next", help="next quoted poem(s) needing full translation")
    sp.add_argument("--count", type=int, default=1)
    sp = sub.add_parser("trans-context", help="a poem's untranslated lines, for the loop to translate")
    sp.add_argument("poem", help="poem_id")
    sp.add_argument("--limit", type=int, default=40, help="max lines per batch (long poems chunk)")
    sp = sub.add_parser("trans-add", help="store per-line English translations")
    sp.add_argument("poem", help="poem_id (for logging)")
    sp.add_argument("--file", required=True)

    sp = sub.add_parser("meter-next", help="base meters still needing a teaching article (JSON)")
    sp.add_argument("--count", type=int, default=99)
    sp = sub.add_parser("meter-context", help="a meter's identity + candidate showcase lines")
    sp.add_argument("key", help="meter slug, e.g. tawil")
    sp.add_argument("--limit", type=int, default=30, help="candidate lines to offer")
    sp = sub.add_parser("meter-add", help="store/refresh a meter's teaching article (pending)")
    sp.add_argument("key", help="meter slug, e.g. tawil")
    sp.add_argument("--file", required=True)

    sub.add_parser("poets-missing", help="distinct poets lacking a Latin romanisation (JSON)")
    sp = sub.add_parser("poets-add", help="apply {poets:[{poet,poet_latin}]}")
    sp.add_argument("--file", required=True)
    sp = sub.add_parser("titles-context", help="poem title + first line context for an id batch")
    sp.add_argument("--ids", required=True, help="comma-separated poem ids")
    sp = sub.add_parser("titles-add", help="apply {titles:[{poem_id,title_en}]}")
    sp.add_argument("--file", required=True)

    args = p.parse_args()

    if args.cmd == "init":
        conn = get_conn(); ensure_schema(conn); conn.close()
        print(f"Schema ready in {DB}")
        return 0
    return {
        "load-muallaqat": cmd_load_muallaqat,
        "load-kaggle": cmd_load_kaggle,
        "tiers": cmd_tiers,
        "index-root": cmd_index_root,
        "index-next": cmd_index_next,
        "index-add": cmd_index_add,
        "verify-root": cmd_verify_root,
        "stats": cmd_stats,
        "trans-next": cmd_trans_next,
        "trans-context": cmd_trans_context,
        "trans-add": cmd_trans_add,
        "meter-next": cmd_meter_next,
        "meter-context": cmd_meter_context,
        "meter-add": cmd_meter_add,
        "poets-missing": cmd_poets_missing,
        "poets-add": cmd_poets_add,
        "titles-context": cmd_titles_context,
        "titles-add": cmd_titles_add,
    }[args.cmd](args)


if __name__ == "__main__":
    sys.exit(main())
